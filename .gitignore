# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Compiled output of the shared type declarations (types/SharedTypes.d.ts is
# source; no .js should ever be emitted there — guard against stray artifacts)
types/*.js

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.db*

# Playwright
test-results/
playwright-report/

# Claude Code files
.claude
research.node_modules

# Planning scratch — subagent review working files are regenerated each run;
# never commit them (the durable plan/review docs alongside them are fine to track)
plans/**/tmp/

# Env files
.env
.env.*

# Stray user dotfiles that leaked into repo root
.bash_profile
.bashrc
.gitconfig
.gitmodules
.mcp.json
.profile
.ripgreprc
.zprofile
.zshrc
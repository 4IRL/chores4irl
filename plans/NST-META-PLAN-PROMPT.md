Read META-PLAN.md. Run the feature ranked <N> (<F-id>) and only that feature. Follow its "Per-Feature Session Contract": cold-survey and verify the repo matches that feature's "Assumed starting state" (STOP and report if it diverges), then create-plan → review-plan → run-plan (once) → git-commit → verify "Expected end state" → git-push. End the session after the PR is pushed. Do not start any other feature.
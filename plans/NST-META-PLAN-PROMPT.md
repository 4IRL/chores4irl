Read META-PLAN.md. Run feature <F-id> and only that feature. Follow its "Per-Feature Session Contract": cold-survey and verify the repo matches that feature's "Assumed starting state" (STOP and report if it diverges), then create-plan → review-plan → run-plan (once) → git-commit → verify "Expected end state" → git-push. After the PR is pushed, stop and wait for explicit user confirmation of two things: (a) the feature shipped correctly on its branch, and (b) its PR has been merged into main — do not assume either. Once both are confirmed, branch again off the now-updated main (e.g. `chore/meta-plan-update-<F-id>`) and update META-PLAN.md per its own Ledger update protocol and History policy: delete this feature's Status ledger row, fold any implemented-contract details a remaining feature still needs into "Completed-Feature Contracts still in force," and refresh the Baseline section to the new main SHA. Commit and git-push that update as its own small PR, then end the session. Do not start any other feature.
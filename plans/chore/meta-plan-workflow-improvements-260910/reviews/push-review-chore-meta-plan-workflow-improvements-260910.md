# Push Review: chore/meta-plan-workflow-improvements-260910

## Review 1
Generated: 2026-09-10 20:49
Comparison: origin/main...HEAD
Verdict: **BLOCKED**

### Results by Reviewer

#### 1. Safety & Security — PASS
Documentation-only diff; no secrets, injection vectors, or unreviewed destructive operations. All branch/remote deletions gated by `AskUserQuestion` checkpoints and default to non-force flags (`-d` not `-D`; `git worktree remove` not `rm -rf`). One minor: `worktree/SKILL.md`'s teardown `-D` path is less explicitly guarded in prose than `compact-plans/SKILL.md`'s unconditional "Never `-D`".

#### 2. Correctness — FAIL
`run-feature/SKILL.md`'s Phase A sequences updating the Status-ledger row with "the PR link" *before* the `/git-push` step that actually creates the PR — contradicts `META-PLAN.md`'s own protocol ("in-review + PR link **after** git-push"). The Phase A/B/C dispatch table also has two real gaps: no row for a closed-but-unmerged PR, and no branch-already-exists fallback for `git checkout -b` despite the skill billing itself as "Resumable" and the table itself anticipating a branch that may already exist. Two minors: teardown's `-D` command is described but never shown; the GREEN/YELLOW/RED aggregation rule for a pair sharing multiple files isn't stated.

#### 3. Simplicity & Conciseness — PASS
All three new skills are notably more concise than the prompt templates they replace (e.g. `worktree/SKILL.md` 87 lines vs. 215-line `WORKTREE-PARALLELIZE-PROMPT.md`) and correctly delegate rather than re-paste. Minor, optional-only: some branch-guard/scope-boundary phrasing is restated 2-3× within a single file.

#### 4. Test Coverage — FAIL
`compact-plans/SKILL.md` drops the explicit end-state verification checklist its predecessor (`COMPACT-PLANS-PROMPT.md`'s "## Output" section) had, closing instead with narrative reporting only — nothing in the procedure re-verifies the sweep actually succeeded, and unlike `run-feature`/`worktree`, its output never routes through `/git-push`'s own review gate. Minor: stray-`.claude`-dir deletion doesn't verify the dir is actually empty first; the findings-harvest step has no completeness check; `worktree`'s per-worktree `npm install`/branch/clean check has no stated failure path.

#### 5. Completeness & Cleanup — PASS
No TODO/stub content; no stale references to the deleted prompt files in any live doc (the two remaining hits are in already-frozen historical review/plan docs, informational only); `worktree` correctly hands off to `/run-feature`.

#### 6. Consistency & Style — FAIL
All three new skills close with an unprecedented `## Constraints` heading — a full grep of the ~40-skill baseline (`urls4irl` + global `~/.claude/skills/`) finds zero precedent; the dominant convention is `## Important Notes`. "Pause-and-ask checkpoint" callouts are formatted inconsistently both against the `new-feature` precedent and against each other within this diff. Branch-guard terminology/heading also disagrees across all three files and matches no baseline convention. Minor: `compact-plans`'s `argument-hint: (none)` has no precedent — every zero-arg skill in the baseline just omits the field.

#### 7. Integration Risk — PASS
The `.gitignore` change correctly un-ignores only `.claude/skills/` (verified with `git check-ignore` — `c4i-app.pem`, `settings.local.json`, `skill-config.md`, `settings.json` all remain ignored). Minor/informational: two already-frozen historical docs still name the deleted prompt files (no live doc affected, no action needed); a one-time bootstrap gap exists where `/worktree` provisioned from a pre-merge `main` won't yet have these skills (self-resolves once this PR merges — worth a one-line PR-description callout, not a code fix).

#### 8. Error Handling & Silent Failures — FAIL
**Critical:** `run-feature/SKILL.md`'s Phase A/B/C dispatch table is built directly from `gh pr list` output with no check of the command's exit status or JSON validity — a `gh` failure (auth/network/rate-limit) is indistinguishable from "no PR found" and would silently route into Phase A's `git checkout -b` (fresh-implementation) path even when a branch/PR already exists. Major: no rule for `gh pr list --state all` returning multiple PRs for one branch; `worktree`'s `git merge-tree` conflict check never inspects the command's own exit status, so a tool failure is silently reported as "clean" — undermining its own "auditable, not asserted" safety claim; `compact-plans`'s remote branch-delete step has vague/non-actionable failure handling (404 vs. 403 vs. transient not distinguished). Minor: unchecked `npm install` failures in `worktree`; `gh pr list` failures not distinguished from "no PR" in `worktree` teardown's confirmation list; `gmas` success never re-verified across all three skills. Also credits real strengths: every destructive op sits behind an explicit pause-and-ask checkpoint, and the `-d`-only/never-`-D`/stop-on-refusal pattern is clean and specific.

### To-Do: Required Changes

- [x] **Fix `run-feature`'s Phase A ledger/push ordering** — `.claude/skills/run-feature/SKILL.md` steps 10-11 — Swap the order: run `/git-push` first (opens the PR), *then* update the Status-ledger row to `in-review` with the now-known PR link as a small follow-up commit — matching `plans/META-PLAN.md`'s own "Ledger update protocol" ("in-review + PR link after git-push").
- [x] **Add a "PR closed, not merged" row to `run-feature`'s dispatch table** — `.claude/skills/run-feature/SKILL.md` phase-resolution table — `gh pr list --head feature/<slug> --state all` can return a closed-and-not-merged PR, which matches none of the four current rows. Add an explicit row (e.g. stop and ask the user whether to reopen, restart on a fresh branch, or abandon the F-id).
- [x] **Add a branch-exists fallback to `run-feature`'s Phase A step 3** — `.claude/skills/run-feature/SKILL.md` — `git checkout -b feature/<slug> main` errors if the branch already exists (a real case the dispatch table itself anticipates: "branch may not exist yet either"). Mirror `worktree/SKILL.md`'s existing pattern (its step 5 already handles "new branch" vs. "branch already exists"), e.g. `git checkout feature/<slug> 2>/dev/null || git checkout -b feature/<slug> main`.
- [x] **Make `run-feature`'s phase-detection distinguish a `gh` failure from "no PR found"** — `.claude/skills/run-feature/SKILL.md` phase-resolution step — Before consulting the dispatch table, check `gh`'s exit code and that stdout parses as valid JSON. On any command failure, stop and report the raw error rather than falling through to Phase A's branch-creation step (a `gh` outage would otherwise silently look identical to "no PR found" and could duplicate work on an already-in-flight feature).
- [x] **Add a multiple-PR-results rule to `run-feature`'s dispatch logic** — `.claude/skills/run-feature/SKILL.md` — `gh pr list --state all` can return more than one PR for the same head branch (e.g. a closed-then-reopened history). Add a rule: use the most recent by number/createdAt; if ambiguous, stop and show the full list to the user rather than guessing.
- [x] **Check `git merge-tree`'s exit status separately from the CONFLICT grep** — `.claude/skills/worktree/SKILL.md` Step 4 empirical check — The current pipeline (`git merge-tree ... | grep -q '^CONFLICT' && ... || echo "clean"`) reports "clean" whenever the grep doesn't match `^CONFLICT` — including when `git merge-tree` itself errors (bad ref, unpushed branch, permissions). Capture the command's own exit code; on a non-zero/error exit distinct from "conflicts found," stop and report the failure instead of defaulting to "clean" (this pipeline backs the skill's own "auditable, not asserted" safety claim for parallelizing worktrees).
- [x] **Spell out remote branch-delete failure handling in `compact-plans`** — `.claude/skills/compact-plans/SKILL.md` Step 6 — The `gh api -X DELETE repos/4IRL/chores4irl/git/refs/heads/<branch>` step says only "confirm it isn't already gone first" with no command given and no guidance on a failed DELETE. Add: check with `gh api repos/4IRL/chores4irl/git/refs/heads/<branch>` first (a 404 there means already-gone, skip); on the DELETE call itself, treat 404 as a benign no-op, and stop + report the branch name and error for any other failure (403/422/5xx) rather than moving on silently.
- [x] **Add a closing verification step to `compact-plans`** — `.claude/skills/compact-plans/SKILL.md` Step 8 — Restore the predecessor `COMPACT-PLANS-PROMPT.md`'s explicit "## Output" checklist as an executable check, not just narrative reporting: before the summary, re-run Step 2's tracking counts and Step 6's `gh pr list --state merged` + `git branch -a` cross-check and confirm they match the intended end state (e.g. no plan dir left live that should have been frozen, no branch left that was confirmed for pruning).
- [x] **Rename `## Constraints` to `## Important Notes` in all three new skills** — `.claude/skills/{run-feature,worktree,compact-plans}/SKILL.md` — Zero precedent for "Constraints" as a closing-section heading across the ~40-skill baseline (`urls4irl` + global `~/.claude/skills/`); the dominant convention (17+ instances) is `## Important Notes`. Rename all three closing sections to match.
- [x] **Standardize "Pause-and-ask checkpoint" callout formatting** — `.claude/skills/{run-feature,worktree,compact-plans}/SKILL.md` — The established precedent (`~/.claude/skills/new-feature/SKILL.md`, 4/4 instances) is `**Pause-and-ask checkpoint — <short label>:**`. Currently `compact-plans` and two of `worktree`'s three instances drop the label; `run-feature` uses a parenthetical instead. Standardize every occurrence to the dash-label form (e.g. "— branch deletion", "— merge confirmation", "— RED/YELLOW pairs", "— teardown confirmation").
- [x] **Standardize branch-guard heading/terminology across all three skills** — `.claude/skills/{run-feature,worktree,compact-plans}/SKILL.md` — 15/16 baseline instances use a dedicated top-level `## Branch Guard` (Title Case) heading. Currently `compact-plans` uses `## 1. Branch guard` (sentence case, numbered), `run-feature` buries it as an inline bold label inside a Phase A list item, and `worktree` never uses the term at all (equivalent content lives under `### 1. Preconditions`). Apply the dominant `## Branch Guard` convention uniformly, including labeling `worktree`'s Preconditions section explicitly as a branch guard.
- [x] **Show both branch-delete commands explicitly in `worktree`'s teardown** — `.claude/skills/worktree/SKILL.md` teardown Step 4 — Only `git branch -d feature/<slug>` is shown, with a trailing comment about `-D`; since a genuinely-abandoned (never-merged) branch is exactly the case `-d` refuses on, show both lines explicitly, e.g. `git branch -d feature/<slug>  # merged` / `git branch -D feature/<slug>  # only if user confirmed abandoned`.
- [x] **State the GREEN/YELLOW/RED aggregation rule for multi-file-shared pairs** — `.claude/skills/worktree/SKILL.md` Step 4 — Add one sentence: a pair's overall verdict is the worst individual verdict across all of its shared files.
- [x] **Verify emptiness before deleting stray `.claude/` dirs in `compact-plans`** — `.claude/skills/compact-plans/SKILL.md` Step 5 — Add an explicit check (e.g. `find plans -type d -name .claude -empty`) before deleting, so a non-empty match falls through to the Step 7 preserve-or-archive judgment instead of being silently removed.
- [x] **Add a findings-harvest completeness check to `compact-plans`** — `.claude/skills/compact-plans/SKILL.md` Step 4 — After copying open `- [ ]` items into `plans/PUSH-REVIEW-FINDINGS.md`, do a quick reconciliation (count in source vs. entries added) and note any mismatch before the source dir is relocated.
- [x] **Add an explicit failure path to `worktree`'s per-worktree check** — `.claude/skills/worktree/SKILL.md` Step 5 — "confirm it's on the right branch and clean" has no stated action if that check fails (e.g. failed `npm install`, wrong branch). Add: stop and report that specific worktree as unusable rather than including it in Step 6's dispatch.
- [x] **Distinguish `gh` failure from "no PR" in `worktree`'s teardown confirmation list** — `.claude/skills/worktree/SKILL.md` teardown Step 2-3 — When `gh pr list` fails for a branch, show it in the Step 3 confirmation as "PR state: UNKNOWN (gh error)" rather than blank/omitted.
- [x] **Re-verify `gmas` succeeded before proceeding** — `.claude/skills/{run-feature,worktree,compact-plans}/SKILL.md` — All three invoke `gmas` to sync `main` with no stated post-check. After `gmas`, re-run `git status` and confirm the tree is clean and matches `origin/main`; on a `gmas` conflict/failure, stop and report rather than continuing.
- [x] **Drop `compact-plans`'s `argument-hint: (none)`** — `.claude/skills/compact-plans/SKILL.md` frontmatter — No precedent in the baseline for a placeholder `argument-hint` value; every other zero-argument skill omits the field entirely. Remove the line.

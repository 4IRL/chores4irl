# Push Review: chore/meta-plan-workflow-improvements-260910

## Review 1
Generated: 2026-09-10 20:49
Comparison: origin/main...HEAD
Verdict: **BLOCKED**

### Results by Reviewer

#### 1. Safety & Security — PASS
Documentation-only diff; no secrets, injection vectors, or unreviewed destructive operations. All branch/remote deletions gated by `AskUserQuestion` checkpoints and default to non-force flags (`-d` not `-D`; `git worktree remove` not `rm -rf`). One minor: `worktree/SKILL.md`'s teardown `-D` path is less explicitly guarded in prose than `compact-plans/SKILL.md`'s unconditional "Never `-D`".

#### 2. Correctness — FAIL
`run-feature/SKILL.md`'s Phase A sequences updating the Status-ledger row with "the PR link" *before* the `/git-push` step that actually creates the PR — contradicts `META-PLAN.md`'s own protocol ("in-review + PR link **after** git-push"). The Phase A/B/C dispatch table also has two real gaps: no row for a closed-but-unmerged PR, and no branch-already-exists fallback for `git checkout -b` despite the skill billing itself as "Resumable" and the table itself anticipating a branch that may already exist. Two minors: teardown's `-D` command is described but never shown; the GREEN/YELLOW/RED aggregation rule for a pair sharing multiple files isn't stated.

#### 3. Simplicity & Conciseness — PASS
All three new skills are notably more concise than the prompt templates they replace (e.g. `worktree/SKILL.md` 87 lines vs. 215-line `WORKTREE-PARALLELIZE-PROMPT.md`) and correctly delegate rather than re-paste. Minor, optional-only: some branch-guard/scope-boundary phrasing is restated 2-3× within a single file.

#### 4. Test Coverage — FAIL
`compact-plans/SKILL.md` drops the explicit end-state verification checklist its predecessor (`COMPACT-PLANS-PROMPT.md`'s "## Output" section) had, closing instead with narrative reporting only — nothing in the procedure re-verifies the sweep actually succeeded, and unlike `run-feature`/`worktree`, its output never routes through `/git-push`'s own review gate. Minor: stray-`.claude`-dir deletion doesn't verify the dir is actually empty first; the findings-harvest step has no completeness check; `worktree`'s per-worktree `npm install`/branch/clean check has no stated failure path.

#### 5. Completeness & Cleanup — PASS
No TODO/stub content; no stale references to the deleted prompt files in any live doc (the two remaining hits are in already-frozen historical review/plan docs, informational only); `worktree` correctly hands off to `/run-feature`.

#### 6. Consistency & Style — FAIL
All three new skills close with an unprecedented `## Constraints` heading — a full grep of the ~40-skill baseline (`urls4irl` + global `~/.claude/skills/`) finds zero precedent; the dominant convention is `## Important Notes`. "Pause-and-ask checkpoint" callouts are formatted inconsistently both against the `new-feature` precedent and against each other within this diff. Branch-guard terminology/heading also disagrees across all three files and matches no baseline convention. Minor: `compact-plans`'s `argument-hint: (none)` has no precedent — every zero-arg skill in the baseline just omits the field.

#### 7. Integration Risk — PASS
The `.gitignore` change correctly un-ignores only `.claude/skills/` (verified with `git check-ignore` — `c4i-app.pem`, `settings.local.json`, `skill-config.md`, `settings.json` all remain ignored). Minor/informational: two already-frozen historical docs still name the deleted prompt files (no live doc affected, no action needed); a one-time bootstrap gap exists where `/worktree` provisioned from a pre-merge `main` won't yet have these skills (self-resolves once this PR merges — worth a one-line PR-description callout, not a code fix).

#### 8. Error Handling & Silent Failures — FAIL
**Critical:** `run-feature/SKILL.md`'s Phase A/B/C dispatch table is built directly from `gh pr list` output with no check of the command's exit status or JSON validity — a `gh` failure (auth/network/rate-limit) is indistinguishable from "no PR found" and would silently route into Phase A's `git checkout -b` (fresh-implementation) path even when a branch/PR already exists. Major: no rule for `gh pr list --state all` returning multiple PRs for one branch; `worktree`'s `git merge-tree` conflict check never inspects the command's own exit status, so a tool failure is silently reported as "clean" — undermining its own "auditable, not asserted" safety claim; `compact-plans`'s remote branch-delete step has vague/non-actionable failure handling (404 vs. 403 vs. transient not distinguished). Minor: unchecked `npm install` failures in `worktree`; `gh pr list` failures not distinguished from "no PR" in `worktree` teardown's confirmation list; `gmas` success never re-verified across all three skills. Also credits real strengths: every destructive op sits behind an explicit pause-and-ask checkpoint, and the `-d`-only/never-`-D`/stop-on-refusal pattern is clean and specific.

### To-Do: Required Changes

- [x] **Fix `run-feature`'s Phase A ledger/push ordering** — `.claude/skills/run-feature/SKILL.md` Phase A steps 9-10 (the `/git-push` step and the ledger-update step immediately after it) — Swap the order: run `/git-push` first (opens the PR), *then* update the Status-ledger row to `in-review` with the now-known PR link as a small follow-up commit — matching `plans/META-PLAN.md`'s own "Ledger update protocol" ("in-review + PR link after git-push").
- [x] **Add a "PR closed, not merged" row to `run-feature`'s dispatch table** — `.claude/skills/run-feature/SKILL.md` phase-resolution table — `gh pr list --head feature/<slug> --state all` can return a closed-and-not-merged PR, which matches none of the four current rows. Add an explicit row (e.g. stop and ask the user whether to reopen, restart on a fresh branch, or abandon the F-id).
- [x] **Add a branch-exists fallback to `run-feature`'s Phase A step 2 (the branch-checkout step)** — `.claude/skills/run-feature/SKILL.md` — `git checkout -b feature/<slug> main` errors if the branch already exists (a real case the dispatch table itself anticipates: "branch may not exist yet either"). Mirror `worktree/SKILL.md`'s existing pattern (its Provision-mode Step 4, "Provision worktrees", already handles "new branch" vs. "branch already exists"), e.g. `git checkout feature/<slug> 2>/dev/null || git checkout -b feature/<slug> main`.
- [x] **Make `run-feature`'s phase-detection distinguish a `gh` failure from "no PR found"** — `.claude/skills/run-feature/SKILL.md` phase-resolution step — Before consulting the dispatch table, check `gh`'s exit code and that stdout parses as valid JSON. On any command failure, stop and report the raw error rather than falling through to Phase A's branch-creation step (a `gh` outage would otherwise silently look identical to "no PR found" and could duplicate work on an already-in-flight feature).
- [x] **Add a multiple-PR-results rule to `run-feature`'s dispatch logic** — `.claude/skills/run-feature/SKILL.md` — `gh pr list --state all` can return more than one PR for the same head branch (e.g. a closed-then-reopened history). Add a rule: use the most recent by number/createdAt; if ambiguous, stop and show the full list to the user rather than guessing.
- [x] **Check `git merge-tree`'s exit status separately from the CONFLICT grep** — `.claude/skills/worktree/SKILL.md` Step 3 ("Pairwise independence matrix") empirical check — The current pipeline (`git merge-tree ... | grep -q '^CONFLICT' && ... || echo "clean"`) reports "clean" whenever the grep doesn't match `^CONFLICT` — including when `git merge-tree` itself errors (bad ref, unpushed branch, permissions). Capture the command's own exit code; on a non-zero/error exit distinct from "conflicts found," stop and report the failure instead of defaulting to "clean" (this pipeline backs the skill's own "auditable, not asserted" safety claim for parallelizing worktrees).
- [x] **Spell out remote branch-delete failure handling in `compact-plans`** — `.claude/skills/compact-plans/SKILL.md` Step 5 ("Prune merged branches") — The `gh api -X DELETE repos/4IRL/chores4irl/git/refs/heads/<branch>` step says only "confirm it isn't already gone first" with no command given and no guidance on a failed DELETE. Add: check with `gh api repos/4IRL/chores4irl/git/refs/heads/<branch>` first (a 404 there means already-gone, skip); on the DELETE call itself, treat 404 as a benign no-op, and stop + report the branch name and error for any other failure (403/422/5xx) rather than moving on silently.
- [x] **Add a closing verification step to `compact-plans`** — `.claude/skills/compact-plans/SKILL.md` Step 7 ("Verify end state, then summary and commit") — Restore the predecessor `COMPACT-PLANS-PROMPT.md`'s explicit "## Output" checklist as an executable check, not just narrative reporting: before the summary, re-run Step 1's ("Tracking check") tracking counts and Step 5's ("Prune merged branches") `gh pr list --state merged` + `git branch -a` cross-check and confirm they match the intended end state (e.g. no plan dir left live that should have been frozen, no branch left that was confirmed for pruning).
- [x] **Rename `## Constraints` to `## Important Notes` in all three new skills** — `.claude/skills/{run-feature,worktree,compact-plans}/SKILL.md` — Zero precedent for "Constraints" as a closing-section heading across the ~40-skill baseline (`urls4irl` + global `~/.claude/skills/`); the dominant convention (17+ instances) is `## Important Notes`. Rename all three closing sections to match.
- [x] **Standardize "Pause-and-ask checkpoint" callout formatting** — `.claude/skills/{run-feature,worktree,compact-plans}/SKILL.md` — The established precedent (`~/.claude/skills/new-feature/SKILL.md`, 4/4 instances) is `**Pause-and-ask checkpoint — <short label>:**`. Currently `compact-plans` and two of `worktree`'s three instances drop the label; `run-feature` uses a parenthetical instead. Standardize every occurrence to the dash-label form (e.g. "— branch deletion", "— merge confirmation", "— RED/YELLOW pairs", "— teardown confirmation").
- [x] **Standardize branch-guard heading/terminology across all three skills** — `.claude/skills/{run-feature,worktree,compact-plans}/SKILL.md` — 15/16 baseline instances use a dedicated top-level `## Branch Guard` (Title Case) heading. Currently `compact-plans` uses `## 1. Branch guard` (sentence case, numbered), `run-feature` buries it as an inline bold label inside a Phase A list item, and `worktree` never uses the term at all (equivalent content lives under `### 1. Preconditions`). Apply the dominant `## Branch Guard` convention uniformly, including labeling `worktree`'s Preconditions section explicitly as a branch guard.
- [x] **Show both branch-delete commands explicitly in `worktree`'s teardown** — `.claude/skills/worktree/SKILL.md` teardown Step 4 — Only `git branch -d feature/<slug>` is shown, with a trailing comment about `-D`; since a genuinely-abandoned (never-merged) branch is exactly the case `-d` refuses on, show both lines explicitly, e.g. `git branch -d feature/<slug>  # merged` / `git branch -D feature/<slug>  # only if user confirmed abandoned`.
- [x] **State the GREEN/YELLOW/RED aggregation rule for multi-file-shared pairs** — `.claude/skills/worktree/SKILL.md` Step 3 ("Pairwise independence matrix") — Add one sentence: a pair's overall verdict is the worst individual verdict across all of its shared files.
- [x] **Verify emptiness before deleting stray `.claude/` dirs in `compact-plans`** — `.claude/skills/compact-plans/SKILL.md` Step 4 ("Purge regenerable scratch") — Add an explicit check (e.g. `find plans -type d -name .claude -empty`) before deleting, so a non-empty match falls through to Step 6's ("Preserve, never discard") preserve-or-archive judgment instead of being silently removed.
- [x] **Add a findings-harvest completeness check to `compact-plans`** — `.claude/skills/compact-plans/SKILL.md` Step 3 ("Freeze + relocate finished plans") — After copying open `- [ ]` items into `plans/PUSH-REVIEW-FINDINGS.md`, do a quick reconciliation (count in source vs. entries added) and note any mismatch before the source dir is relocated.
- [x] **Add an explicit failure path to `worktree`'s per-worktree check** — `.claude/skills/worktree/SKILL.md` Step 4 ("Provision worktrees") — "confirm it's on the right branch and clean" has no stated action if that check fails (e.g. failed `npm install`, wrong branch). Add: stop and report that specific worktree as unusable rather than including it in Step 5's ("Dispatch") dispatch list.
- [x] **Distinguish `gh` failure from "no PR" in `worktree`'s teardown confirmation list** — `.claude/skills/worktree/SKILL.md` teardown Step 2-3 — When `gh pr list` fails for a branch, show it in the Step 3 confirmation as "PR state: UNKNOWN (gh error)" rather than blank/omitted.
- [x] **Re-verify `gmas` succeeded before proceeding** — `.claude/skills/{run-feature,worktree,compact-plans}/SKILL.md` — All three invoke `gmas` to sync `main` with no stated post-check. After `gmas`, re-run `git status` and confirm the tree is clean and matches `origin/main`; on a `gmas` conflict/failure, stop and report rather than continuing.
- [x] **Drop `compact-plans`'s `argument-hint: (none)`** — `.claude/skills/compact-plans/SKILL.md` frontmatter — No precedent in the baseline for a placeholder `argument-hint` value; every other zero-argument skill omits the field entirely. Remove the line.

## Review 2
Generated: 2026-09-10 21:41
Comparison: origin/main...HEAD
Verdict: **BLOCKED**

### Results by Reviewer

#### 1. Safety & Security — PASS
Documentation-only diff; no secrets, no injection vectors, and every destructive git/gh operation (branch delete, remote ref delete, worktree removal) is either non-force (`-d`, `git worktree remove`) or sits behind an explicit `AskUserQuestion` pause-and-ask checkpoint before execution. Minor: `worktree/SKILL.md`'s teardown `-D` path is less explicitly guarded in-line than `compact-plans/SKILL.md`'s unconditional "Never `-D`" wording.

#### 2. Correctness — FAIL
`compact-plans/SKILL.md`'s Step 5 branch-prune snippet doesn't actually link `git branch -d`'s exit status into the remote-delete guard — if `-d` refuses, the script as written still proceeds to check and delete the remote ref, silently bypassing the one safety net `-d` (vs. `-D`) is supposed to provide (major). Two minors: Review 1's own To-Do citations reference step numbers that drifted during renumbering and no longer match the final SKILL.md files; `run-feature/SKILL.md`'s Phase C step 2 (`git checkout -b chore/meta-plan-update-<f-id-lowercase>`) has no branch-exists fallback, unlike Phase A's step 2 which was fixed for exactly this resumability case.

#### 3. Simplicity & Conciseness — PASS
The three new skills remain substantially more concise than the prompt templates they replace; all deleted-template references were updated. Minor: `run-feature/SKILL.md` restates its `gmas` post-sync verification sentence verbatim in both Branch Guard and Phase C step 1 rather than cross-referencing.

#### 4. Test Coverage — PASS
No application test suite applies to this doc-only diff; the three skills' own verification/reconciliation steps are substantially strengthened over their predecessors. Minor: `compact-plans/SKILL.md`'s Step 7 end-state verification re-checks tracking counts and branch pruning but never re-verifies the freeze-header was actually prepended, ledger retention left the right files, or the `.gitignore` entry landed; `compact-plans` is also the only one of the three skills whose terminal action is `/git-commit` alone with no `/git-push` review gate, a gap Review 1 flagged but didn't turn into a checklist item.

#### 5. Completeness & Cleanup — PASS
No debug content, TODO/stub/TBD markers, or dangling references to the four deleted prompt-template files anywhere live in the repo. Minor: same stale step-number citations in this review file's own To-Do list as flagged by Correctness — cosmetic, since every cited fix is independently verified as actually applied.

#### 6. Consistency & Style — FAIL
`worktree/SKILL.md`'s Teardown mode renders its 5 steps as a bare numbered list with no heading markup, while Provision mode in the same file uses `### N. Title` headings for the same kind of content — a structural inconsistency within a freshly-authored file (major). Minors: `compact-plans/SKILL.md` is missing a blank line after every heading, unlike its two sibling files; `SUPERSEDED` is bolded in `worktree.md` but plain text in `run-feature.md`; "Pause-and-ask checkpoint" callouts still vary in follow-up formatting (prose vs. bare list vs. code block) in `worktree.md` and `run-feature.md`; `plans/META-PLAN.md`'s newly-edited sentence mixes `F-id` and `F-IDs` casing.

#### 7. Integration Risk — PASS
All four deleted prompt templates are fully de-referenced from `META-PLAN.md` and every other live doc/skill; the `.gitignore` change was verified with `git check-ignore` to track only `.claude/skills/*` while keeping credentials/settings ignored. No other skill or doc assumes the old step-numbering/headings. Minor/informational only: a self-resolving bootstrap gap for worktrees provisioned before this PR merges, and one historical narrative log line naming a deleted file — neither needs a code fix.

#### 8. Error Handling & Silent Failures — FAIL
**Critical:** the same `compact-plans/SKILL.md` Step 5 gap as Correctness — `git branch -d`'s exit status is never inspected before the remote-delete block runs, so the documented "if `-d` refuses, stop and investigate" behavior isn't actually enforced by the shown bash. **Major:** the DELETE call's own exit status is also never captured — only the 404-existence pre-check is a real `if`; the "403/422/5xx: stop and report" behavior exists only in prose. Minors: `run-feature/SKILL.md` Phase A step 10's "confirm the push actually succeeded" names no concrete check; `gh` failure handling doesn't distinguish a transient rate-limit from a permanent auth failure. Well-handled by contrast: `worktree/SKILL.md`'s `git merge-tree` check correctly captures `$?` separately and distinguishes clean/conflict/tool-failure; `run-feature`'s dispatch table now covers closed-PR and multi-PR-result cases and treats a `gh` failure as a hard stop; every destructive operation sits behind an explicit pause-and-ask checkpoint.

### To-Do: Required Changes

- [x] **Gate `compact-plans`' remote branch-delete on the local `-d` succeeding** — `.claude/skills/compact-plans/SKILL.md` Step 5 (branch-pruning loop) — `git branch -d <branch>` is followed by an unconditional `if gh api .../refs/heads/<branch> ...; then gh api -X DELETE ...; fi` with no `&&`/`set -e`/`$?` check linking them, so a refused local delete (the branch isn't actually fully merged) doesn't stop the remote delete from running anyway. Chain them, e.g. `if git branch -d <branch>; then <existence-check-then-delete>; else echo "STOP: git branch -d refused for <branch> — investigate before any remote action"; fi`.
- [x] **Capture the `gh api -X DELETE` call's own exit status in `compact-plans`** — `.claude/skills/compact-plans/SKILL.md` Step 5 — only the 404-existence pre-check uses a real `if`; the prose-asserted "403/422/5xx: stop and report" behavior has no matching check on the DELETE line itself. Add `if ! gh api -X DELETE repos/4IRL/chores4irl/git/refs/heads/<branch>; then echo "STOP: remote delete failed for <branch> — report and do not continue to the next branch"; fi`, mirroring the existence-check's own `if` style one line above.
- [x] **Give `worktree`'s Teardown mode step headings matching Provision mode** — `.claude/skills/worktree/SKILL.md` Teardown mode (currently a bare `1.`–`5.` list) — Provision mode in the same file uses `### N. Title` headings for equivalent content; apply the same heading style to Teardown mode's 5 steps.
- [x] **Add a branch-exists fallback to `run-feature`'s Phase C step 2** — `.claude/skills/run-feature/SKILL.md` Phase C — `git checkout -b chore/meta-plan-update-<f-id-lowercase>` has no fallback if that branch already exists (e.g. a Phase C attempt interrupted before its own PR merged, then resumed). Mirror Phase A step 2's pattern: `git checkout chore/meta-plan-update-<f-id-lowercase> || git checkout -b chore/meta-plan-update-<f-id-lowercase> main`.
- [x] **Fix stale step-number citations in Review 1's To-Do list** — `plans/chore/meta-plan-workflow-improvements-260910/reviews/push-review-chore-meta-plan-workflow-improvements-260910.md` (Review 1 section) — several "Step N"/"step N" citations reference numbers that no longer match the final renumbered SKILL.md files (e.g. run-feature "steps 10-11" → actually 9-10; worktree "Step 4"/"Step 5" for content that's actually in Step 3/Step 4; compact-plans "Step 6"/"Step 8" for content actually in Step 5/Step 7). Re-point each citation to its actual final location, or soften to a section-title reference so future renumbering can't invalidate it again. Low priority — purely a historical audit-trail accuracy issue; every underlying fix is already applied and verified.
- [x] **Name a concrete post-push verification check in `run-feature` Phase A** — `.claude/skills/run-feature/SKILL.md` Phase A step 10 — "confirm the push actually succeeded" names no concrete check, unlike the `gmas` re-verification pattern used elsewhere in the same doc. Name one, e.g. "confirm via `git status` (nothing to push) or by checking `git push`'s own exit status/output."
- [x] **Add a blank line after every heading in `compact-plans`** — `.claude/skills/compact-plans/SKILL.md` — every `##`/`###` heading is immediately followed by body text with no blank line, unlike `run-feature.md` and `worktree.md` which both consistently insert one. Add the missing blank lines to match.
- [x] **Standardize `SUPERSEDED` emphasis across `run-feature` and `worktree`** — `.claude/skills/run-feature/SKILL.md`, `.claude/skills/worktree/SKILL.md` — plain text in one file, bolded (`**SUPERSEDED**`) in the other, for the same concept. Bold it in both, matching `worktree.md` and `META-PLAN.md`'s own SUPERSEDED banners.
- [x] **Add a lead-in sentence to the remaining bare-list/code-block Pause-and-ask checkpoint instances** — `.claude/skills/worktree/SKILL.md` (~line 60), `.claude/skills/run-feature/SKILL.md` (~line 60) — these two instances drop straight into a bullet list / fenced code block with no lead-in sentence, unlike the `new-feature.md` baseline pattern (label + inline prose) that the rest of this diff's checkpoints already follow. Add a short lead-in sentence to each.
- [x] **Fix `F-id`/`F-IDs` casing mix in `META-PLAN.md`** — `plans/META-PLAN.md` (~line 468) — the same sentence uses lowercase `F-id` in the invocation example and uppercase `F-IDs` in the next clause. Use lowercase `F-ids` throughout, matching the skills' own convention.
- [x] **Clarify `worktree` teardown's `-D` command only runs after explicit confirmation** — `.claude/skills/worktree/SKILL.md` Teardown Step 4 — add a one-line reminder next to the `-D` command that it only ever runs after Step 3's `AskUserQuestion` confirms abandonment, matching `compact-plans/SKILL.md`'s more explicit unconditional "Never `-D`" phrasing.
- [x] **Broaden `compact-plans`' Step 7 end-state verification** — `.claude/skills/compact-plans/SKILL.md` Step 7 — the checklist re-checks tracking counts and branch pruning but never re-verifies that Step 3's freeze header was actually prepended to each relocated plan, that Step 4's ledger retention left the right files, or that Step 4's `.gitignore` entry landed. Add one line covering each.
- [x] **Decide and document `compact-plans`' relationship to `/git-push`** — `.claude/skills/compact-plans/SKILL.md` Step 7 — `compact-plans` is the only one of the three skills whose terminal action is `/git-commit` alone, with no `/git-push` review gate. Either add a closing `/git-push` step to match `run-feature`/`worktree`, or explicitly state in Step 7 that this is a deliberate choice rather than an oversight.
- [x] **Reduce `gmas` post-check duplication in `run-feature`** — `.claude/skills/run-feature/SKILL.md` — the `gmas` post-sync verification sentence is restated verbatim in both Branch Guard and Phase C step 1. Have Phase C step 1 cross-reference Branch Guard's pattern instead of repeating the full sentence, or factor it into one reusable line.

## Review 3
Generated: 2026-09-11 06:24
Comparison: origin/main...HEAD
Verdict: **BLOCKED**

### Results by Reviewer

#### 1. Safety & Security — PASS
Docs-only diff remains clean: no secrets or injection vectors; every destructive git/gh op (`branch -d`/`-D`, remote ref delete, `worktree remove`) is force-free and gated by `AskUserQuestion`. The `.gitignore` change was independently re-verified with `git check-ignore -v` — only `.claude/skills/*.md` is tracked; `c4i-app.pem`, `settings.json`, `settings.local.json`, `skill-config.md` all remain ignored. No findings.

#### 2. Correctness — PASS
All Review 1 and Review 2 To-Do items verified as genuinely landed in the final SKILL.md content (ledger/push ordering, dispatch-table gaps, `gh`/`merge-tree` exit-status checks, compact-plans' `-d`-then-remote-delete gating and DELETE exit capture, teardown headings, step citations). Independently re-verified the `.gitignore` re-inclusion and the GitHub `git/refs/heads/<branch>` GET/DELETE semantics (exact-match 200, no-match 404) against the live repo. One new minor: `compact-plans` Step 7's ledger-retention re-check uses a bare `ls plans/ledger/*_feature_ledger.md` glob, which errors if the dir is missing/empty — the same class of problem the freeze-header check one line above deliberately avoids with `find ... 2>/dev/null | xargs -r`.

#### 3. Simplicity & Conciseness — PASS
All three new skills remain substantially more concise than the four templates they replace (96/88/107 lines vs. 118/1/57/215); the stale hardcoded "shared surfaces" table was correctly dropped rather than carried over; no dead branches in the bash snippets. Two minors: the checkout-or-create error-visibility caveat is restated verbatim in `run-feature`'s Phase A step 2 and Phase C step 2 (the same pattern Review 2 fixed for the `gmas` sentence); the post-`gmas` verification sentence is duplicated near-verbatim across all three skills' Branch Guard sections (low priority — each SKILL.md is read standalone).

#### 4. Test Coverage — FAIL
Reviews 1-2's self-verification fixes (compact-plans Step 7 checklist, Step 3 harvest reconciliation, worktree `merge-tree` exit-status check, per-worktree failure path) are all present and correct. Two new **major** gaps: (a) `worktree`'s Teardown mode ends with no closing verification — after `git worktree remove` and, for confirmed-abandoned branches, `git branch -D` (a force-delete with no refusal path), nothing cross-checks `git worktree list` / `git branch -a` against Step 3's confirmed list, unlike `compact-plans` Step 7 which re-verifies its own branch pruning; (b) `run-feature` Phase C step 3 prescribes four distinct `META-PLAN.md` edits (delete ledger row, annotate ID map, add/fold contract, refresh Baseline) plus a conditional ★FOCUS re-evaluation with no reconciliation before step 4's `/git-commit` — a partial application would ride into the commit undetected. Minor: `compact-plans` Step 7 never re-confirms Step 4's `tmp/` purge actually happened for the just-frozen plans.

#### 5. Completeness & Cleanup — PASS
No TODO/stub content introduced; all 31 Review 1 + Review 2 `[x]` items spot-checked against the final SKILL.md text and confirmed applied; skill frontmatter complete and accurate; no scratch/tmp files committed. Three minors: two pre-existing dangling references to the deleted prompt templates live outside `plans/completed|abandoned` (`plans/feature/touch-lock/touch-lock.md` ~line 599 names `REFRESH-META-PLAN-PROMPT.md`; `plans/chore/meta-plan-housekeeping-260723/reviews/push-review-*.md` ~line 31 names `COMPACT-PLANS-PROMPT.md`) — both historical narrative in finished-but-not-yet-archived dirs, not introduced by this diff; and `compact-plans` Step 5's `# remote delete` inline comment merely restates the command.

#### 6. Consistency & Style — PASS
All Review 1 and Review 2 style fixes (`## Important Notes`, Pause-and-ask dash-label form, `## Branch Guard`, `**SUPERSEDED**`, `F-id`/`F-ids` casing, blank lines after headings, Teardown `### N. Title` headings, `-D` guard wording) verified landed uniformly. Heading-hierarchy variance between the three skills was checked against the baseline and found justified by each file's differing structure. One new minor: `run-feature` Phase B's merge-confirmation checkpoint is the only one in the diff rendered as a fenced pseudo-UI block (`AskUserQuestion, multiSelect: ... [ ] ...`) rather than inline prose/bullets — defensible for a 2-item multiSelect, but the sole exception.

#### 7. Integration Risk — PASS
`.gitignore` correctly un-ignores only `.claude/skills/*`; every `META-PLAN.md` section/heading the skills reference resolves with matching wording; all five invoked global skills (`/git-commit`, `/git-push`, `/plan-creator`, `/plan-reviewer`, `/run-plan`) exist and their `reviews/push-review-<branch>.md` convention matches; hard-coded `repos/4IRL/chores4irl` matches `skill-config.md`; no live doc references the deleted templates. Two minors: `run-feature` Phase A steps 5-6 invoke `/plan-reviewer` and `/run-plan` without showing the argument, though both skills fuzzy-match a required plan name (`$0`/`$ARGUMENTS`); and `/plan-creator`'s own Branch Guard fires an `AskUserQuestion` whenever not on `main`, so every Phase A run (already on `feature/<slug>` from step 2) will hit an avoidable "stay on current branch" prompt.

#### 8. Error Handling & Silent Failures — FAIL
All 17 prior To-Do items confirmed present and correct (compact-plans' `-d` gating and DELETE exit check; worktree's `merge-tree` `$?` capture; run-feature's `gh` exit/JSON check, closed-PR and multi-PR rows, checkout fallbacks preserving first-attempt errors; teardown `UNKNOWN (gh error)`; `gmas` re-verification). Two new: **Critical** — `compact-plans` Step 5's remote-ref existence pre-check (`gh api .../refs/heads/<branch> >/dev/null 2>&1`) collapses every non-zero exit to "already gone": a 403, 5xx, rate-limit, or network blip is indistinguishable from a genuine 404 once stderr is discarded, so the DELETE is silently skipped with no output and the sweep reports the branch as pruned when its remote ref was never checked — the same `>/dev/null 2>&1`-hides-a-real-error pattern the adjacent DELETE line was deliberately fixed to avoid. **Major** — `run-feature` Phase C step 2 instructs a rebase/merge of a pre-existing `chore/meta-plan-update-*` branch onto freshly-synced `main`, and its own prose names the concurrent-ledger-edit risk that makes a conflict plausible, yet gives no instruction for when that rebase/merge actually conflicts — an unsupervised auto-resolution is exactly the "clobber their ledger/Baseline updates" outcome the step is trying to prevent. Minor: `compact-plans` Step 3's `git mv`/`mv` relocation has no in-the-moment failure path (Step 7's live-dir re-check is only an end-of-run safety net).

### To-Do: Required Changes

- [x] **Distinguish a genuine 404 from other `gh` failures in `compact-plans`' remote-ref existence check** — `.claude/skills/compact-plans/SKILL.md` Step 5 ("Prune merged branches"), the `if gh api repos/4IRL/chores4irl/git/refs/heads/<branch> >/dev/null 2>&1; then` line — `>/dev/null 2>&1` collapses 403/5xx/rate-limit/network failures into the same non-zero exit as a real 404, so any of them silently skips the DELETE with no message and the sweep reports the branch as pruned. Either capture the response/exit and only treat it as benign on an actual 404 (e.g. `gh api ... 2>&1` and inspect for `HTTP 404`, or `gh api -i` / `--include` to read the status line), echoing a `STOP: existence check failed for <branch> — <error>` for any other failure — or drop the pre-check entirely and rely on the DELETE call's own response, since `gh api -X DELETE` on an already-gone ref also returns 404 and the existing 404-vs-other-failure branching on the DELETE line already handles it. Update the prose sentence "If the existence check itself returns 404, the ref is already gone — skip the DELETE, a benign no-op" to match whichever form is chosen.
- [x] **Add a conflict-handling path to `run-feature`'s Phase C step 2 rebase/merge** — `.claude/skills/run-feature/SKILL.md` Phase C step 2 — after "rebase/merge it onto this freshly-synced `main` now, before step 3 edits `plans/META-PLAN.md`", add: if the rebase/merge reports a conflict, stop, run `git rebase --abort` (or `git merge --abort`), and report the conflicting branch/commit and files to the user rather than resolving it automatically — the user decides how to reconcile the two features' ledger/Baseline edits before step 3 proceeds. An unsupervised auto-resolution of a ledger/Baseline conflict is exactly the "clobber their ledger/Baseline updates" outcome the step's own prose warns about.
- [x] **Add a closing verification step to `worktree`'s Teardown mode** — `.claude/skills/worktree/SKILL.md` Teardown mode, after Step 5 ("Prune stale admin entries") — add a `### 6. Verify end state` step: re-run `git worktree list` and `git branch -a`, cross-check both against Step 3's confirmed removal list, and confirm (a) every confirmed worktree path and branch is gone and (b) no worktree/branch outside that list was touched. Flag any mismatch before reporting. This mirrors `compact-plans` Step 7's re-verification of its own Step 5 branch pruning and matters most for the `git branch -D` path, which force-deletes unmerged work with no refusal safety net.
- [x] **Add a pre-commit reconciliation to `run-feature`'s Phase C step 3 META-PLAN edits** — `.claude/skills/run-feature/SKILL.md` Phase C, between step 3 (the four `META-PLAN.md` edits) and step 4 (`/git-commit`) — add an explicit check that every prescribed edit actually landed: the F-id's Status-ledger row no longer appears; its Legacy → current ID map row shows the PR number (if applicable); either "COMPLETED-FEATURE CONTRACTS STILL IN FORCE" or Baseline / Standing invariants gained the new content; and the Baseline header (`main` at PR #N, `<SHA>`) changed. Reconcile any gap before committing, matching the self-verification pattern Phase A step 8 already uses for "Expected end state". A partially-applied edit set would otherwise ride into the commit undetected and leave `META-PLAN.md` internally inconsistent.
- [x] **Use a tolerant form for `compact-plans` Step 7's ledger-retention check** — `.claude/skills/compact-plans/SKILL.md` Step 7 ("Verify end state"), the `ls plans/ledger/*_feature_ledger.md` bullet — a bare glob errors with "No such file or directory" if `plans/ledger/` is missing or has no matches, the exact problem the freeze-header check one line above avoids with `find ... 2>/dev/null | xargs -r`. Replace with `find plans/ledger -maxdepth 1 -name '*_feature_ledger.md' 2>/dev/null` (and note that zero matches at this point is itself a flag, since Step 4 should never fully empty the ledger).
- [x] **Add a `tmp/`-purge check to `compact-plans` Step 7** — `.claude/skills/compact-plans/SKILL.md` Step 7 ("Verify end state") — add one bullet confirming Step 4's purge actually happened for the plans frozen this run, e.g. `find plans/completed plans/abandoned -mindepth 2 -maxdepth 2 -type d -name tmp 2>/dev/null` returns nothing for the dirs relocated in this sweep; a skipped purge would otherwise leave regenerable review-dimension scratch under the archive, contradicting the skill's own "no regenerable scratch" goal.
- [x] **Add an in-the-moment failure path to `compact-plans` Step 3's relocation** — `.claude/skills/compact-plans/SKILL.md` Step 3 ("Freeze + relocate finished plans"), the "Then relocate: `git mv <dir> plans/completed/` …" sentence — add one clause: if the `git mv`/`mv` fails (destination already exists, permissions, etc.), report the dir name, destination, and error immediately rather than relying on Step 7's end-of-run live-dir check to surface it.
- [x] **Cross-reference instead of restating the checkout-fallback caveat in `run-feature`'s Phase C step 2** — `.claude/skills/run-feature/SKILL.md` Phase C step 2 — the "Don't suppress the first attempt's error output — … masked by the fallback's own error" sentence is verbatim from Phase A step 2. Keep the full caveat in Phase A step 2 and shorten Phase C step 2 to e.g. "same checkout-or-create fallback and error-visibility caveat as Phase A step 2", retaining only the Phase-C-specific rebase/merge addendum (and the conflict-handling clause added above).
- [x] **Show the argument to `/plan-reviewer` and `/run-plan` in `run-feature` Phase A** — `.claude/skills/run-feature/SKILL.md` Phase A steps 5 and 6 — both global skills locate the plan by fuzzy-matching a required name argument (`/plan-reviewer`: `$0`; `/run-plan`: `$ARGUMENTS`), unlike `/plan-creator` which infers its topic. Make the invocations explicit, e.g. `/plan-reviewer <slug>` and `/run-plan <slug>`, so the session doesn't have to guess which string to supply.
- [x] **Note `/plan-creator`'s expected Branch Guard prompt in `run-feature` Phase A step 4** — `.claude/skills/run-feature/SKILL.md` Phase A step 4 — `/plan-creator`'s own Branch Guard fires an `AskUserQuestion` ("Switch to main first" vs "Stay on current branch") whenever it's not on `main`, and Phase A step 2 has already checked out `feature/<slug>`, so every run hits it. Add a parenthetical that this prompt is expected and the answer is always "stay on current branch" (changing `plan-creator`'s guard itself is out of scope for this PR).
- [x] **Decide the Phase B checkpoint's format in `run-feature`** — `.claude/skills/run-feature/SKILL.md` Phase B ("human merge gate") — the merge-confirmation checkpoint is the only one in the diff rendered as a fenced pseudo-UI block (`AskUserQuestion, multiSelect: … [ ] …`); every other checkpoint uses inline prose/bullets. Either convert it to prose (e.g. "Ask via `AskUserQuestion` (multiSelect) whether both are true: the feature works correctly on its branch, and its PR is merged into `main`.") or leave the block and add a short note that the checklist form is deliberate for this multiSelect case.
- [x] **Replace `compact-plans`' `# remote delete` comment with a why** — `.claude/skills/compact-plans/SKILL.md` Step 5, the `gh api -X DELETE …` line — the comment restates the command; the sibling comment one line above (`# -d only — refuses anything not actually merged`) explains a non-obvious *why*. Drop it or replace with e.g. `# only reached if -d succeeded above`.
- [x] **Trim the duplicated post-`gmas` sentence across the three skills' Branch Guards (optional)** — `.claude/skills/{compact-plans,run-feature,worktree}/SKILL.md` Branch Guard sections — "After `gmas`, re-run `git status` and confirm the tree is clean and matches `origin/main`; on a `gmas` conflict/failure, stop and report rather than continuing" appears near-verbatim in all three. Each SKILL.md is read standalone so a cross-file reference isn't practical; optionally shorten each to a one-clause reminder (e.g. "synced (`gmas`; verify with `git status` that it's clean and matches `origin/main`, stop on any conflict)"). Low priority — skip if the explicit form is preferred.
- [x] **Defer two pre-existing dangling references to the next `/compact-plans` sweep** — `plans/feature/touch-lock/touch-lock.md` (~line 599, names `REFRESH-META-PLAN-PROMPT.md`), `plans/chore/meta-plan-housekeeping-260723/reviews/push-review-chore-meta-plan-housekeeping-260723.md` (~line 31, names `COMPACT-PLANS-PROMPT.md`) — no edit in this PR: both are historical narrative in finished-but-not-yet-archived dirs, not instructions to open the deleted files, and were not introduced by this diff. They become archive-historical once `/compact-plans` freezes those dirs into `plans/completed/`. Check this item off after confirming no live instruction depends on either file.

## Review 4
Generated: 2026-09-11 12:46
Comparison: origin/main...HEAD
Verdict: **BLOCKED**

### Results by Reviewer

#### 1. Safety & Security — PASS
Docs-only diff: no secrets, injection vectors, or unguarded destructive ops. Independently re-verified on disk that `.gitignore`'s `.claude/*` + `!.claude/skills/` tracks only the three SKILL.md files while `c4i-app.pem`, `settings.json`, `settings.local.json`, `skill-config.md`, and `generate-gh-c4i-token.sh` stay ignored, and that every branch/remote/worktree deletion is non-force and behind an `AskUserQuestion` checkpoint. One minor: `worktree` Teardown Step 4's `-D` path is the sole reachable force-delete in the diff — properly gated, but worth cross-referencing as the one exception to the "never force" rule used elsewhere (folded into To-Do item 2 below, which rewrites that prose anyway).

#### 2. Correctness — FAIL
**Critical:** `compact-plans` Step 5 and `worktree` Teardown Step 4 both treat `git branch -d`'s exit status as mechanical proof that a branch is "actually merged" into `main`. Per `git-branch(1)`, `-d` checks merge status against the branch's *configured upstream* when one is set and falls back to HEAD only when there is none. Every branch produced by this workflow gets an upstream via `/git-push`'s `git branch --set-upstream-to=origin/$BRANCH`, so `-d` checks against `origin/<branch>` — trivially satisfied for anything pushed — not against `main`. Verified empirically by the reviewer (pushed branch + non-ancestor squash of its content onto `main` → `-d` deleted with only a warning), and confirmed against this repo's live state by the orchestrator: `feature/clear-input-buttons` still tracks `origin/feature/clear-input-buttons` and its PR #34 squash-merged (not an ancestor of `main`), so `-d` would succeed vacuously. The flip side compounds it: once the upstream is pruned (`git fetch -p` after GitHub's auto-delete-on-merge), `-d` falls back to HEAD and *refuses every squash-merged branch* — so with the skill's "Never `-D`" rule it could never prune the three stale local branches META-PLAN.md explicitly asks it to. Either way the `-d` gate described as a second safety net is not one; the only real gate is the PR-state cross-check plus the human confirmation. Major: the identical claim in `worktree` Teardown Step 4 ("`-d` — refuses if it isn't actually merged"). All other checks (gh 404/422 semantics, `merge-tree` exit handling, `.gitignore`, META-PLAN cross-references, Reviews 1-3 items) verified correct.

#### 3. Simplicity & Conciseness — PASS
The three skills remain far more concise than the templates they replace. Three minors, all accumulated prose-on-top-of-bash from prior fix rounds: `compact-plans` Step 5's post-snippet paragraph re-narrates what the `if/elif/else` comments and `STOP:` echoes already say; Step 7 states the tolerant-`find`-vs-bare-glob rationale twice in near-identical wording (lines 88 and 90); `worktree` Teardown Step 6's closing paragraph adds `git fsck --lost-found` recovery trivia that changes nothing the agent does.

#### 4. Test Coverage — PASS
All Review 1-3 self-verification fixes independently re-verified as landed; `run-feature` still gates on the Vitest + `e2e/smoke.spec.ts` suites before push. Two new minors, both in `compact-plans`: the Branch Guard's "On a feature branch already: proceed" path has no clean-tree check (unlike `run-feature`/`worktree`), so unrelated uncommitted changes could ride into Step 7's `/git-commit`; and Step 2's / Step 5's `gh pr list --state merged` calls have no failure handling, unlike the exit-code/JSON check `run-feature` applies to the same command (degrades gracefully — fewer plans classified/pruned — but inconsistent).

#### 5. Completeness & Cleanup — PASS
No debug/TODO/stub content or committed scratch; the only dangling template references are the two deliberately deferred in Review 3; all skill→META-PLAN.md cross-references resolve; frontmatter accurate; all 31+ checked-off Review 1-3 items spot-checked as genuinely landed.

#### 6. Consistency & Style — FAIL
Two verified majors. (a) Casing: the new skills and the new META-PLAN.md sentences use lowercase `F-id`/`F-ids` (11 in `run-feature`, 14 in `worktree`, 6 in META-PLAN.md lines 342-362), contradicting the established `F-ID`/`F-IDs` convention in META-PLAN.md's own untouched text (5 instances: lines 15, 43, 263, 413, 419 — e.g. "**F-ID scheme (fixed at the 2026-07-07 reconcile…)**") and in the sibling global skill `~/.claude/skills/new-feature/SKILL.md` (17 instances, 0 lowercase). Review 2's `fdd13f4` fix made one sentence internally consistent but in the wrong direction — the document is now split against itself. (b) All twelve `### N. Title` step headings in `worktree/SKILL.md` (lines 19, 25, 28, 50, 59, 69, 77, 80, 83, 86, 97, 102) are followed directly by body text with no blank line — a regression against the convention Review 2's `15d781b` standardized for `compact-plans`, and which every heading in `compact-plans`, `run-feature`, and `worktree`'s own four H2s already follow. Review 2's write-up asserted `worktree` was compliant; it wasn't for its H3s.

#### 7. Integration Risk — PASS
No findings. `.gitignore` negation verified (`git check-ignore -v`, `git ls-files .claude`); no live doc/README/CI/script references the deleted templates or the old blanket ignore; `run-feature`/`worktree` assumptions about `/plan-creator`'s Branch Guard wording, `/plan-reviewer` and `/run-plan` fuzzy plan-name arguments, and `/git-push`'s review path all match the global skill files verbatim; `package.json` workspaces and vite/tsconfig aliases are relative, so sibling `../c4i-wt-<slug>` worktrees work; every cited META-PLAN.md protocol/heading exists with matching wording.

#### 8. Error Handling & Silent Failures — PASS
All 17 error-handling items from Reviews 1-3 re-verified present in the on-disk text with no regressions. One new minor: `worktree` Teardown Step 4's `git worktree remove` line carries only `# refuses if dirty — resolve first, don't force`, with no explicit stop-and-report instruction, unlike the `git branch -d` line directly below it; a dirty-tree refusal is eventually caught by Step 6, but the asymmetric phrasing could read as license to discard uncommitted changes unilaterally.

### To-Do: Required Changes

- [x] **Replace `git branch -d`'s exit status as the merged-into-`main` gate in `compact-plans` Step 5** — `.claude/skills/compact-plans/SKILL.md` Step 5 ("Prune merged branches"), the bash block starting `if git branch -d <branch>; then   # -d only — refuses anything not actually merged` and the prose paragraph after it (`Never \`-D\`. If \`-d\` refuses, stop and investigate…` through `…whose local \`-d\` was refused`) — `-d` checks merged-ness against the branch's configured upstream (`origin/<branch>`, set by `/git-push`), not `main`, so it succeeds trivially for any pushed branch; and once that upstream is pruned it falls back to HEAD and refuses every squash-merged branch, so "Never `-D`" would make the skill unable to prune the very branches META-PLAN.md asks it to. Make the PR-merged check the real gate: immediately before deleting, re-verify per branch with `gh pr view <number> --json state,mergedAt` (from Step 5's `gh pr list --state merged` result) and confirm the `mergeCommit` is on `main` (`git merge-base --is-ancestor <mergeCommit> main`); keep the `AskUserQuestion` confirmation; then delete locally with `git branch -D <branch>` (recoverable via the `(was <sha>)` line / reflog until gc) and proceed to the existing remote existence-check + DELETE flow only if the local delete succeeded. Rewrite the inline comment and prose to say the gate is the PR/merge-commit verification + human confirmation, that `-d`'s refusal is *expected* for a squash-merged branch and is not diagnostic, and drop the "Never `-D`" / "`-d` refuses anything not actually merged" claims. (Alternative if `-D` is unacceptable: keep `-d` but state plainly that it only guards local-branch reversibility, not merge status, and accept that pruned-upstream branches can't be deleted by this skill.) Update the `## Important Notes` bullet and any `-d`/`-D` mention in Step 7's verification bullets to match.
- [x] **Apply the same `-d` correction to `worktree` Teardown Step 4** — `.claude/skills/worktree/SKILL.md` Teardown Step 4 ("Remove confirmed worktrees and branches"), the `**Merged** → \`git branch -d feature/<slug>\` — refuses if it isn't actually merged…` bullet, the `**User-confirmed abandoned**` bullet, and the `Never run \`-D\` on any other classification` sentence — same defect: `-d` checks against `origin/feature/<slug>` (always set by `/run-feature`'s `/git-push`), so it won't refuse a branch whose PR never merged, and it *will* refuse a genuinely squash-merged one once the upstream is pruned. Drop the "refuses if it isn't actually merged" claim; re-derive "Merged" immediately before deletion with `gh pr view <number> --json state,mergedAt` (Step 2 already has the PR number), and after that check plus Step 3's confirmation use `git branch -D feature/<slug>` for both classifications, noting `-D` is reachable only after PR-verified-merged or user-confirmed-abandoned (this also addresses Safety's minor about cross-referencing the sole force-delete). Update Step 6's "`-d`/`-D` with the `(was <sha>)` it printed" and the `-D`-specific sentences to match.
- [x] **Standardize `F-id`/`F-ids` → `F-ID`/`F-IDs` across the new skills and the new META-PLAN.md sentences** — `.claude/skills/run-feature/SKILL.md` (11 occurrences, including frontmatter `argument-hint: <F-id>` and the description), `.claude/skills/worktree/SKILL.md` (14 occurrences, including `argument-hint` and the Step 1 heading "Resolve and filter the F-id set"), `plans/META-PLAN.md` lines 342, 344, 350 (×3), 362 — match the capital-`ID` convention in META-PLAN.md's pre-existing text (lines 15, 43, 263, 413, 419) and `~/.claude/skills/new-feature/SKILL.md` (17 instances). Leave the branch-name placeholder `<f-id-lowercase>` in `run-feature` Phase C step 2 as is (it describes a lowercased slug, not the term). This reverses the direction of Review 2's `fdd13f4` fix, which is the one that should be overridden.
- [x] **Add a blank line after each of the twelve `### N. Title` headings in `worktree`** — `.claude/skills/worktree/SKILL.md` lines 19, 25, 28, 50, 59, 69, 77, 80, 83, 86, 97, 102 — every one is followed directly by body text; insert one blank line after each, matching the file's own H2 headings and every heading in `compact-plans` and `run-feature`.
- [x] **Add a clean-tree check to `compact-plans`' Branch Guard feature-branch path** — `.claude/skills/compact-plans/SKILL.md` Branch Guard, the sentence `On a feature branch already: proceed.` — mirror `run-feature`'s guard: run `git status` and, if the tree is dirty, stop and ask how to proceed rather than continuing, so unrelated uncommitted changes can't ride into Step 7's `/git-commit`.
- [x] **Add `gh` failure handling to `compact-plans`' `gh pr list --state merged` calls** — `.claude/skills/compact-plans/SKILL.md` Step 2 ("Classify every plan", the **Merged** bullet) and Step 5 (the `gh pr list --state merged --json …` block) — add one sentence matching `run-feature`'s pattern: check `gh`'s exit code and that stdout parses as valid JSON before using the result; on failure stop and report the raw error rather than treating an empty result as "nothing merged" (which would silently under-classify and under-prune).
- [x] **Make `git worktree remove`'s refusal path explicit in `worktree` Teardown Step 4** — `.claude/skills/worktree/SKILL.md` Teardown Step 4, the `git worktree remove ../c4i-wt-<slug>   # refuses if dirty — resolve first, don't force` line — reword to match the sibling `-d` line's explicitness, e.g. `# refuses if dirty; if it refuses, stop and report the worktree path and error — never force-clean or discard uncommitted changes to make it succeed`.
- [x] **Trim `compact-plans` Step 5's post-snippet prose to what the bash doesn't already say** — `.claude/skills/compact-plans/SKILL.md` Step 5, the paragraph after the bash block — after item 1's rewrite, keep only: the 422-vs-404 rationale for the separate existence pre-check, and the "Never prune: the default branch / unmerged commits / **Live** feature or open PR" list. Drop the sentences that re-narrate the `if/elif/else` branches' own comments and `STOP:` echoes (the "benign only on an actual `HTTP 404`…", "On the DELETE call: any failure (403/422/5xx) — stop…" sentences).
- [x] **State the tolerant-`find` rationale once in `compact-plans` Step 7** — `.claude/skills/compact-plans/SKILL.md` Step 7 ("Verify end state"), the freeze-header bullet (keeps the full "`find`/`xargs -r` form tolerates either directory not existing yet, unlike a bare glob" parenthetical) and the ledger-retention bullet two lines below — shorten the latter's closing parenthetical to a cross-reference such as "(same tolerant-`find` rationale as the freeze-header check above)".
- [x] **Trim `worktree` Teardown Step 6's recovery trivia** — `.claude/skills/worktree/SKILL.md` Teardown Step 6 ("Verify end state"), the closing paragraph's "a deleted local branch is recoverable until gc from the `(was <sha>)` line … or via `git fsck --lost-found` — its reflog is gone once the worktree is pruned, so `git reflog` won't find it" — reduce to one clause (e.g. "a deleted local branch is recoverable from its `(was <sha>)` line only until gc, and its reflog is gone once the worktree is pruned"); drop the `git fsck --lost-found` detail the step never directs the agent to use.
- [x] **[Added during /run-review from item 6's Security reviewer — not a Review 4 reviewer item] Add an explicit `--limit` to `compact-plans`' three `gh pr list --state merged` calls** — `.claude/skills/compact-plans/SKILL.md` Step 2 (the **Merged** bullet), Step 5 (the candidate-list bash block), and Step 7 (the re-run bullet that quotes Step 5 verbatim) — `gh pr list` silently caps at 30 results by default and this repo already has 32 merged PRs (verified: the default call returns 30 with lowest PR #3, `--limit 500` returns 32 with lowest PR #1), so PRs #1-#2 were dropped from classification and pruning while item 6's exit-code/JSON checks still passed on the truncated-but-valid result; add `--limit 500` to all three identically and one sentence at the Step 2 site explaining why the flag is load-bearing. The `--head <branch>`-filtered calls in `worktree` Teardown Step 2 and `run-feature`'s "Decide which phase to resume" are left unchanged — filtered to a single head branch, the cap would need 30+ PRs from one branch to bite.

## Review 5
Generated: 2026-09-13 21:11
Comparison: origin/main...HEAD
Verdict: **BLOCKED**

### Results by Reviewer

#### 1. Safety & Security — PASS
Docs-only diff: no secrets or credential exposure (re-verified via `git check-ignore`/`git ls-files` that `.claude/*` + `!.claude/skills/` tracks only the three SKILL.md files while `c4i-app.pem`, `settings.json`, `settings.local.json`, `skill-config.md`, and `generate-gh-c4i-token.sh` stay ignored); every destructive git/gh op (`-D`, remote ref DELETE, `worktree remove`) sits behind a real PR-merge/merge-base verification plus an `AskUserQuestion` checkpoint. One minor hardening note: `<branch>`/`feature/<slug>` are interpolated into jq program text (`.headRefName == "<branch>"`) and `gh api` URL strings rather than passed as a jq variable — git ref names may legally contain `"`, `\`, `$`, `;`; exploitability is low since this tooling generates every branch name it touches.

#### 2. Correctness — PASS
Bash gating, jq filters, gh flags/endpoints, `.gitignore` semantics, and all internal Step/Phase cross-references across the three skills and META-PLAN.md verified correct; several edge cases tested empirically (`git worktree remove` on an already-pruned worktree, `git branch -D` refusing a still-checked-out branch, the `.vscode` pattern interaction — a pre-existing condition, not a regression). One minor: `worktree` Teardown Step 4's `git worktree remove` → `git branch -D` pair is two unguarded sequential lines whose "skip the `-D` below on refusal" behavior lives only in a comment — not exploitable today (git refuses to `-D` a branch still checked out in the un-removed worktree, confirmed live), but unlike every sibling gate in the diff it isn't encoded as control flow.

#### 3. Simplicity & Conciseness — PASS
All Review 4 trims verified landed; cross-references resolve to current step numbers; the `-D`-gate bash is dense but justified. Two minors: `compact-plans` Step 5's `git branch -D` inline comment (line 64) restates the full upstream-vs-`main` rationale the paragraph on line 83 already carries (the sibling `worktree` line 113 uses `# -D on purpose — see below`); `worktree` line 113 still restates the recoverability clause that Step 6 (line 130) states in full.

#### 4. Test Coverage — FAIL
All Review 1–4 self-verification fixes re-verified present. **Major:** `compact-plans` Step 5's "shipped via a *different* path" branch-deletion carve-out (line 56) routes around the PR/merge-base gate with narrative only ("confirm its work is genuinely on `main` first … delete it only outside that snippet") — no example command, nothing Step 7 can cross-check, and the riskier default compared to `worktree`'s "Neither → leave in place"; orchestrator note: it also directly contradicts the same step's own rule on line 83 ("Never prune: … any branch whose PR has not merged") and `## Important Notes` line 108 ("the PR/merge-commit/tip verification plus that confirmation is the only gate"). Minors: `run-feature` Phase A steps 3 and 10 promise Status-ledger row edits with no re-verification (Phase C step 4 got exactly this after Review 3); `worktree` Teardown Step 4's unconditional `-D` after an unchecked `worktree remove` (same as Correctness's minor); `compact-plans` Step 7 never re-checks Step 5's "delete that Status-ledger row too" promise.

#### 5. Completeness & Cleanup — PASS
All 58 Review 1–4 To-Do items verified genuinely applied against the live files; no stale live references to the four deleted templates (the two remaining hits are the historical-narrative ones deferred in Review 3); no TODO/placeholder content; no committed scratch; every Step/Phase cross-reference resolves. One minor, same site as Test Coverage's major: the Step 5 "different path" carve-out never names the deletion commands to use out-of-band — folded into that item.

#### 6. Consistency & Style — PASS
Frontmatter shape/order, `F-ID` casing, Branch Guard / Important Notes naming, blank-line-after-heading, Pause-and-ask dash-label form, SKIP:/STOP: convention, and the shared `gmas`-verification and jq merged-PR snippets all verified consistent across the three skills and against the global baseline. Two cosmetic minors: `compact-plans` line 27 says plain-text "SUPERSEDED banners" where `run-feature`:13 and `worktree`:22 bold `**SUPERSEDED**`; `run-feature`'s frontmatter `description` lacks the inline "Never …" scope-boundary clause its two siblings both carry before "Use when".

#### 7. Integration Risk — FAIL
**Critical:** `worktree` Provision Step 4 only runs `git worktree add` + `npm install`; it never provisions the untracked `.claude/` config. This diff's own `.gitignore` (`.claude/*` / `!.claude/skills/`) keeps `.claude/skill-config.md` untracked, and worktrees materialize tracked files only — orchestrator verified `.claude/skill-config.md` is ignored on both `main` and this branch and the skill text contains no `skill-config`/`.claude`/copy/symlink mention. Every worktree hands off to `/run-feature`, whose Phase A step 9 runs `/git-push`, whose Step 1/Step 5 `exit 1` when `repo` is unset (`/plan-creator` also reads `plans_dir`/`topic_inference` from the same file) — so every worktree-driven `/run-feature` hard-fails at push, after plan/review/implement/commit have already run; worst in Mode B where no human is watching. **Major:** `playwright.config.ts` hardcodes `webServer` on `localhost:3000`/`5174` with `reuseExistingServer: !process.env.CI`; two worktrees whose Phase A step 8 smoke runs overlap will have the second silently adopt the first's already-listening dev servers and verify the wrong worktree's build (CI on GitHub sets `CI` and remains a real backstop, which keeps this from critical).

#### 8. Error Handling & Silent Failures — FAIL
All 17+ prior error-handling fixes re-verified on disk. **Majors:** (a) `compact-plans` Step 5's `git fetch origin` (line 60) has no exit-status check before the loop's `merge-base --is-ancestor … origin/main` trusts `origin/main`; orchestrator note: `origin` is SSH and this session's own preflight `git push --dry-run` failed with `Permission denied (publickey)` in the Bash tool's non-interactive shell (the reason `/git-push` fetches via HTTPS+token), so the unchecked fetch is the *default* outcome here, not a rare blip — the same unchecked `git fetch origin` sits in `worktree` Teardown Step 4 (line 98). Stale `origin/main` can only make the gate fail-safe, but (b) that failure then surfaces as the final `else`'s `SKIP: could not verify <branch> (PR not merged from it, gh error, merge commit not on origin/main, or local tip beyond the PR head)` (line 80) — four causes in one message; a `gh pr view` failure (auth/rate-limit — ~2 calls per candidate) is indistinguishable from a legitimately-unmerged branch, unlike the list-building `gh pr list` calls two lines above which stop+report on command failure. (c) `run-feature` Phase C step 2's checkout-or-create fallback (line 67) can fail both ways and leave the session on `main`; Phase A step 4 has an explicit "if that prompt does not appear you are still on `main` — stop" check for the identical pattern, Phase C has none, and step 4's `git diff main -- plans/META-PLAN.md` checks content not branch (caught late by `/git-commit`'s own guard, which the skill doesn't state or rely on). Minors: Step 3 line 41 says "stop and report … then move on to the next plan" — "stop" for a per-item SKIP contradicts the file's own SKIP/STOP vocabulary; `worktree` Step 4 line 57 never says to check `git worktree add`'s own exit status; `run-feature` Phase C step 6 lacks Phase A step 9's "If it rejects, fix per its findings and re-push" clause. Well-handled: the rebase-conflict abort-and-report path, `merge-tree` exit capture, 404-vs-other remote-ref distinction, and the `gh pr list` exit/JSON checks.

### To-Do: Required Changes

- [x] **Provision the untracked `.claude/skill-config.md` into every worktree in `worktree` Provision Step 4** — `.claude/skills/worktree/SKILL.md` Step 4 ("Provision worktrees"), the sentence "Then, in each worktree: `npm install` …" — worktrees materialize tracked files only, and this branch's `.gitignore` (`.claude/*` / `!.claude/skills/`) keeps `.claude/skill-config.md` untracked, so a fresh `../c4i-wt-<slug>` has no `.claude/` dir at all; `/run-feature` → `/git-push` then `exit 1`s on the missing `repo:` field (and `/plan-creator` silently loses `plans_dir`/`topic_inference`) after the whole plan/implement/commit pipeline has already run. Add, alongside `npm install`: `mkdir -p ../c4i-wt-<slug>/.claude && ln -s "$(git rev-parse --show-toplevel)/.claude/skill-config.md" ../c4i-wt-<slug>/.claude/skill-config.md` (symlink, so edits in the main checkout stay in sync; do the same for `.claude/settings.json` if the project relies on it for sandbox scope), and **never** copy or symlink the whole `.claude/` directory — it holds `c4i-app.pem` and `generate-gh-c4i-token.sh`. Add "`test -f ../c4i-wt-<slug>/.claude/skill-config.md`" to the same step's per-worktree "confirm it's on the right branch and clean" check so a worktree missing it is reported unusable and excluded from Step 5's dispatch; and add one `## Important Notes` bullet naming why the symlink exists so a future edit doesn't drop it.
- [x] **Force fresh dev servers for smoke runs inside a worktree so sibling worktrees can't silently share ports** — `.claude/skills/run-feature/SKILL.md` Phase A step 8 ("Verify 'Expected end state'") and `.claude/skills/worktree/SKILL.md` Step 5 ("Dispatch") / Step 6 ("Summary") — `playwright.config.ts` hardcodes `webServer` on `localhost:3000` and `localhost:5174` with `reuseExistingServer: !process.env.CI`, so when two worktrees' smoke runs overlap (Mode B by design; Mode A whenever the user drives two terminals) the second run adopts the first worktree's already-listening servers and verifies the wrong build. In run-feature step 8 add: when the checkout is a `/worktree`-provisioned `../c4i-wt-<slug>`, run `e2e/smoke.spec.ts` with `CI=1` so `reuseExistingServer` is off — Playwright then fails loudly ("… is already used …") instead of silently reusing a sibling's server, and that failure means wait for the sibling's run to finish and retry, not skip verification. In worktree Step 5's Mode B instruction and Step 6's merge-order reminder add a one-line pointer to that rule. Parameterizing the ports in `playwright.config.ts` is app code, out of scope for this docs-only PR — note it as an optional follow-up F-ID rather than editing the config here.
- [x] **Drop the "shipped via a different path" branch-deletion carve-out from `compact-plans` Step 5** — `.claude/skills/compact-plans/SKILL.md` Step 5, line 56, the two sentences from "A branch whose functionality shipped via a *different* path …" through "… after the on-`main` spot-check and its own explicit confirmation." — it authorizes a `-D` + remote DELETE outside the PR/merge-base snippet on narrative judgment alone, names no command or check, is invisible to Step 7's PR-driven re-verification, and contradicts the same step's own "Never prune: … any branch whose PR has not merged" (line 83) and `## Important Notes`' "the PR/merge-commit/tip verification plus that confirmation is the only gate" (line 108). Delete the two sentences and, in their place, one clause stating such branches are left alone by this skill and reported under "left in place" for the user to handle manually (matching `worktree` Teardown's "Neither → leave in place" default). If the carve-out is kept instead, it must get its own mechanical check (e.g. a named `git log origin/main --oneline -- <files the plan touched>` or `git diff origin/main..<branch> --stat` showing nothing unique) before its confirmation, plus a Step 7 bullet that re-verifies it — but removal is the recommended fix.
- [x] **Check `git fetch origin`'s exit status before trusting `origin/main` in `compact-plans` Step 5 and `worktree` Teardown Step 4** — `.claude/skills/compact-plans/SKILL.md` line 60 ("Run `git fetch origin` once first so `origin/main` is current …") and `.claude/skills/worktree/SKILL.md` line 98 (same sentence) — neither says to check the fetch succeeded before the per-branch `git merge-base --is-ancestor "$merged_sha" origin/main` gate runs against it, and in this repo `origin` is SSH, which fails with `Permission denied (publickey)` from the Bash tool's non-interactive shell (the reason `/git-push` Step 1 fetches via HTTPS+token) — so an unchecked fetch is the expected outcome, and every branch then SKIPs with the ambiguous "could not verify" message. At both sites: run the fetch as `git fetch origin || { echo "STOP: git fetch origin failed — origin/main may be stale; fix the fetch (SSH agent unavailable in this shell — run it from a terminal, or fetch `+refs/heads/main:refs/remotes/origin/main` via the HTTPS+token form `/git-push` Step 1 uses) before pruning anything"; }` and state that on that STOP the loop must not start.
- [x] **Distinguish a `gh pr view` command failure from a legitimately-unmerged branch in `compact-plans` Step 5's per-branch gate (and `worktree` Teardown Step 4's copy)** — `.claude/skills/compact-plans/SKILL.md` lines 62 and 80; `.claude/skills/worktree/SKILL.md` lines 100 and 104 — `read -r merged_sha head_oid < <(gh pr view … --jq 'select(…)')` yields empty vars both when `gh` fails (auth/rate-limit/network — ~2 calls per candidate) and when the `select` finds no match, so the final `else`'s "SKIP: could not verify <branch> (PR not merged from it, gh error, merge commit not on origin/main, or local tip beyond the PR head)" hides an environment failure inside a wall of identical SKIPs, unlike the list-building `gh pr list` two lines above which stops on command failure. Capture the call separately: `if ! pr_json=$(gh pr view <number> --json state,mergedAt,mergeCommit,headRefName,headRefOid 2>&1); then echo "STOP: gh pr view #<number> failed — $pr_json"; <halt the sweep>; fi` then feed `$pr_json` to `jq -r 'select(…)'` for the `read`, and split the remaining gate so the SKIP names which check was false (no merged PR from this branch / merge commit not on `origin/main` / local tip beyond PR head). Keep `STOP` for the gh failure and `SKIP` for the three legitimate non-merged outcomes, per the file's own convention; apply identically in both files so the snippets stay in lockstep.
- [x] **Add an on-branch check after `run-feature` Phase C step 2's checkout-or-create fallback** — `.claude/skills/run-feature/SKILL.md` Phase C step 2 (line 67) — `git checkout chore/meta-plan-update-<f-id-lowercase> || git checkout -b … main` can fail both ways (e.g. the branch is checked out in another worktree) and leave the session on `main` from step 1; Phase A step 4 has an explicit "if that prompt does *not* appear, you are still on `main` — stop and report" for the identical pattern, but Phase C has none, and step 4's `git diff main -- plans/META-PLAN.md` checks content, not branch. Add one sentence at the end of step 2: "Confirm `git branch --show-current` prints `chore/meta-plan-update-<f-id-lowercase>` before step 3; if it still prints `main`, both checkouts failed — stop and report their errors rather than editing `META-PLAN.md` on `main`."
- [x] **Pass branch names to jq as a variable instead of interpolating them into the filter text** — `.claude/skills/compact-plans/SKILL.md` line 62 and `.claude/skills/worktree/SKILL.md` line 100 — `.headRefName == "<branch>"` puts the branch name inside a jq string literal, and git ref names may legally contain `"`, `\`, `$`, `;`; use `--arg b "<branch>"` with `select(… .headRefName == $b)` (or, after the previous item's restructuring, `jq --arg b "<branch>" -r '…'`) in both snippets. Low risk since this tooling generates every branch it touches — hardening only.
- [x] **Shorten the two `git branch -D` inline comments to a cross-reference** — `.claude/skills/compact-plans/SKILL.md` line 64 and `.claude/skills/worktree/SKILL.md` line 113 — compact-plans' comment restates the whole upstream-vs-`main` rationale the paragraph on line 83 already carries; worktree's restates the `(was <sha>)` recoverability clause Step 6 (line 130) states in full. Reduce both to `# -D on purpose — see below` and let the paragraph / Step 6 be the single statement of each rationale.
- [x] **Encode `worktree` Teardown Step 4's "skip the `-D` on refusal" as control flow** — `.claude/skills/worktree/SKILL.md` lines 112–113 — the two removal lines are unguarded sequential commands whose skip behavior lives only in line 112's comment; git does refuse to `-D` a branch still checked out in the un-removed worktree (verified), but every sibling gate in the diff uses real `if`/`then`. Wrap them: `if git worktree remove ../c4i-wt-<slug>; then git branch -D "feature/<slug>"; else echo "SKIP: git worktree remove refused for feature/<slug> — report the path and git's error under Step 6's 'left in place'; continue to the next confirmed entry"; fi`, moving the never-`--force`/unlock/stash/discard prohibition into the `else` comment so it isn't lost.
- [x] **Re-verify the Status-ledger row edits in `run-feature` Phase A steps 3 and 10** — `.claude/skills/run-feature/SKILL.md` lines 45 and 52 — each promises a specific `plans/META-PLAN.md` row change (`in-progress`, then `in-review` + PR link) but neither is checked before it rides into a commit, unlike Phase C step 4's reconciliation added after Review 3. Add one clause to each: re-grep the F-ID's Status-ledger row and confirm it now reads `in-progress` (step 3) / `in-review` with the correct PR URL (step 10) before proceeding.
- [x] **Add a Status-ledger-row cleanup check to `compact-plans` Step 7** — `.claude/skills/compact-plans/SKILL.md` Step 7 ("Verify end state"), after the Step 5 re-run bullet (line 99) — Step 5 line 85 promises "delete that Status-ledger row too" for each pruned branch's feature, and Step 7 checks every other Step 4/5 sub-item but not this one. Add one bullet: for each branch confirmed-pruned this run, `grep -n '<F-ID>' plans/META-PLAN.md` under the Status ledger should return no row.
- [x] **Say "SKIP", not "stop", for the per-plan relocation failure in `compact-plans` Step 3** — `.claude/skills/compact-plans/SKILL.md` line 41, "If the `git mv`/`mv` fails … stop and report the dir name, destination, and error immediately … then move on to the next plan rather than aborting the whole sweep" — "stop" for a continue-with-next action contradicts the SKIP:/STOP: vocabulary Step 5 establishes. Reword to "report the dir name, destination, and error immediately (don't wait for Step 7's live-dir check), then SKIP this plan and continue with the next one — do not abort the sweep."
- [x] **Check `git worktree add`'s own exit status in `worktree` Provision Step 4** — `.claude/skills/worktree/SKILL.md` line 57 (the `git worktree add …` snippet) and the following "If either check fails …" sentence — the failure path covers `npm install`, wrong branch, and dirty tree but not the `worktree add` itself (path already exists from an aborted run, branch-name collision). Extend the sentence: "if `git worktree add` itself fails, stop and report that F-ID as unusable — do not proceed to `npm install` or the per-worktree checks for it."
- [x] **Bold `**SUPERSEDED**` in `compact-plans` Step 2** — `.claude/skills/compact-plans/SKILL.md` line 27, "check `META-PLAN.md`'s SUPERSEDED banners" — `run-feature`:13 and `worktree`:22 bold the same META-PLAN.md marker; match them.
- [x] **Add the inline scope-boundary clause to `run-feature`'s frontmatter description** — `.claude/skills/run-feature/SKILL.md` line 3 (`description:`) — `compact-plans` ("Never edits app source or plans a feature.") and `worktree` ("Never plans or implements a feature itself.") both carry a "Never …" clause immediately before "Use when …"; `run-feature`'s one-feature-per-invocation rule lives only in the body. Add "Never starts a second feature in one invocation." in the same position.
- [x] **Add the "if it rejects" clause to `run-feature` Phase C step 6** — `.claude/skills/run-feature/SKILL.md` line 76, "`/git-push` — this is a small docs-only PR; the 8-agent review still runs, that's fine." — Phase A step 9's parallel push step says "If it rejects, fix per its findings and re-push"; append the same sentence here.

## Review 6
Generated: 2026-09-14 09:03
Comparison: origin/main...HEAD
Verdict: **BLOCKED**

### Results by Reviewer

#### 1. Safety & Security — PASS
Docs-only diff; every destructive git/gh op (`git branch -D`, remote ref DELETE, `git worktree remove`) is gated behind a real PR-merge/merge-base verification plus an `AskUserQuestion` checkpoint; `.gitignore` correctly narrows tracking to `.claude/skills/` while `c4i-app.pem` and local config stay ignored; no secrets in the diff; Review 5's jq injection concern is fixed via `--arg`. Two minors at the same site: `compact-plans` Step 5 still interpolates `<branch>` directly into the `gh api` URL strings (line 82 GET, line 85 DELETE) rather than validating/parameterizing it the way the jq call two lines above now does — low exploitability since every branch name is tooling-generated.

#### 2. Correctness — PASS
No findings. Bash snippets in all three skills verified (gh pr view/list JSON handling, jq `--arg` filtering, merge-base/merge-tree exit-code capture, the `git worktree add` pre-check, `main_root` symlink resolution); every Step/Phase cross-reference, META-PLAN.md section reference, and the `.gitignore` `.claude/*` / `.vscode` negation semantics resolve correctly.

#### 3. Simplicity & Conciseness — PASS
No findings. The nested gates each correspond to a distinct, previously-flagged failure mode; the duplicated ~35-line gh/jq/merge-base gate in `compact-plans` Step 5 and `worktree` Teardown Step 4 is necessary (each SKILL.md must stand alone) and was accepted in prior rounds; explanatory prose is load-bearing "why", not restated "what".

#### 4. Test Coverage — PASS
Verification coverage is thorough: nearly every destructive/state-changing step is backed by an explicit re-check or STOP/SKIP control flow, all "verified on git X" claims are encoded, nothing from the deleted templates lost its verification equivalent, and no app source or Playwright test is touched. One minor: `compact-plans` Step 4's stray-empty-`.claude/` deletion (line 46) is the only Step 4 sub-action with no Step 7 re-check (its siblings — tmp/ purge, ledger retention, `.gitignore` entry — each get one).

#### 5. Completeness & Cleanup — PASS
No findings. All 74 To-Do items across Reviews 1–5 verified genuinely landed; no TODO/placeholder content; the two remaining `PROMPT.md` mentions in META-PLAN.md are pre-existing historical narrative outside this diff; nothing from the four deleted templates was dropped without an equivalent (often stronger) replacement.

#### 6. Consistency & Style — PASS
Frontmatter, F-ID/META-PLAN.md/**SUPERSEDED** terminology, SKIP:/STOP: vocabulary, Pause-and-ask checkpoint formatting, Branch Guard headings, `/skill-name` cross-references, and Important Notes closings all consistent. Three cosmetic minors: `worktree` lines 42–43 use POSIX `[ ]` where every other test in both skills uses `[[ ]]`; `compact-plans` bolds **Live** on every mention but Merged/Abandoned/Superseded only at their defining bullets (lines 32, 117 plain); `worktree` line 51 bolds **RED**/**YELLOW** but leaves GREEN and the second YELLOW plain in the same sentence.

#### 7. Integration Risk — FAIL
**Major:** `worktree` Provision Step 4's `git worktree add ../c4i-wt-<slug> …` (lines 58/60) and Teardown Step 4's `git worktree remove ../c4i-wt-<slug>` (line 142) write to a *sibling* of the repo, outside this project's sandbox `filesystem.allowWrite` (`/home/rmila/Code/chores4irl` + `/tmp` — orchestrator confirmed against this session's own sandbox config). Reviewer verified empirically that a write to `/home/rmila/Code/` fails with `Read-only file system` under the default sandbox. Neither command is marked `dangerouslyDisableSandbox: true` and the skill never says to, so every Provision `add` and Teardown `remove` fails by default — and Step 4's failure-handling sentence lists only per-F-ID causes (leftover dir, branch checked out elsewhere), so an agent would misdiagnose the structural sandbox denial as a collision. Orchestrator note: the same sibling-dir write applies to Step 4's two `ln -s … ../c4i-wt-<slug>/.claude/…` calls and the in-worktree `npm install`. Skill invocation contracts, `.gitignore` tracking (`git check-ignore`/`git ls-files` re-verified), and skill-name collisions with the global baseline all check out clean. One minor: `/worktree`'s name and "set up worktrees" trigger phrasing overlap the built-in `EnterWorktree` tool's natural-language trigger — an explicit `/worktree <F-ID>` invocation is unambiguous; only prose requests without the slash command are at risk.

#### 8. Error Handling & Silent Failures — PASS
All prior error-handling fixes re-verified on disk; STOP vs SKIP used correctly throughout; fetch/gh/jq/merge-base failures all surface with context. Two downstream-mitigated minors: `worktree` Teardown Step 4 line 143's `git branch -D` inside the `worktree remove` success branch has no exit check (compact-plans' equivalent at line 81 does) — caught later by Step 6's `git branch -a` re-check but with no inline error; `compact-plans` Step 4's three cleanup actions (tmp/ delete, `.gitignore` add, old-ledger delete) state no in-the-moment failure path, unlike Step 3's `git mv` clause — fully caught by Step 7's re-verification.

### To-Do: Required Changes

- [x] **Run every command that writes into `../c4i-wt-<slug>` with `dangerouslyDisableSandbox: true` in `worktree`** — `.claude/skills/worktree/SKILL.md` Provision Step 4 (the `git worktree add` snippet, lines 56–61; the two `ln -s` calls, lines 68–69; the in-worktree `npm install`, line 73) and Teardown Step 4 (the `git worktree remove` snippet, lines 142–147) — the worktree is a sibling of the repo (`/home/rmila/Code/c4i-wt-<slug>`), outside this project's sandbox `filesystem.allowWrite` (`/home/rmila/Code/chores4irl` + `/tmp`), so under the default sandbox each of these fails with `Read-only file system`, and Step 4's failure sentence would steer the agent to a per-F-ID misdiagnosis (leftover dir / branch checked out elsewhere). Add one sentence before the Provision Step 4 snippet and one before the Teardown Step 4 removal snippet stating that these specific Bash calls must run with `dangerouslyDisableSandbox: true` because the target is outside the project's `allowWrite` (matching CLAUDE.md's "use `dangerouslyDisableSandbox` on specific Bash calls that require it" rule for this repo); keep every other command in the skill (`git worktree list`, `git rev-parse`, `git merge-tree`, `gh pr view`, `test -f`, `git branch -D`, `git worktree prune`) sandboxed as-is since they write only inside the repo or not at all. Add a matching `## Important Notes` bullet naming why the flag is needed, and note there that Step 5 Mode A's hand-off (`cd ../c4i-wt-<slug> && claude`) is unaffected — a fresh session's own cwd is allow-written by the harness. Do not instruct editing `.claude/settings.json`'s `allowWrite` from within the skill — that file is on the sandbox's deny-write list.
- [ ] **Add a Step 7 re-check for Step 4's stray-empty-`.claude/` deletion in `compact-plans`** — `.claude/skills/compact-plans/SKILL.md` Step 7 ("Verify end state"), after the `tmp/` purge bullet (line 112) — Step 4 line 46 deletes empty `plans/**/.claude/` dirs but is the only Step 4 sub-action Step 7 never confirms. Add one bullet: "Confirm Step 4's stray-dir cleanup landed: `find plans -type d -name .claude -empty` should return nothing — a match means the delete was skipped for that dir, so remove it now (a non-empty `.claude/` match is Step 6's preserve-or-archive judgment, not a Step 7 fix)."
- [ ] **Check `git branch -D`'s exit status inside `worktree` Teardown Step 4's removal snippet** — `.claude/skills/worktree/SKILL.md` line 143 — the `-D` sits unguarded inside the `if git worktree remove …; then` branch; if it fails after a successful remove (lock, permissions), nothing is printed and the loop continues, unlike `compact-plans` Step 5 line 81's guarded `-D`. Replace line 143 with `if ! git branch -D "feature/<slug>"; then echo "SKIP: local delete failed for feature/<slug> after its worktree was removed — branch left behind; report the path and git's error under Step 6's \"left in place\", and continue to the next confirmed entry"; fi` (keeping the `# -D on purpose — see below` comment), so the failure is reported at the point it happens rather than only re-discovered by Step 6's `git branch -a` check.
- [ ] **Add an in-the-moment failure clause to `compact-plans` Step 4's cleanup actions** — `.claude/skills/compact-plans/SKILL.md` Step 4 ("Purge regenerable scratch"), after the ledger-retention bullet (line 48) — the three deletes/edits (tmp/ dirs, `.gitignore` entry, old ledger files) have no stated failure path, unlike Step 3's `git mv` clause; Step 7 catches the outcome but with no inline error. Add one sentence: "If any of these deletes or the `.gitignore` edit fails, report the path and error immediately (don't wait for Step 7's re-check), then continue with the remaining Step 4 actions."
- [ ] **Validate `<branch>` before interpolating it into the two `gh api` URL strings in `compact-plans` Step 5** — `.claude/skills/compact-plans/SKILL.md` lines 82 (`gh api "repos/4IRL/chores4irl/git/ref/heads/<branch>"`) and 85 (`gh api -X DELETE "repos/4IRL/chores4irl/git/refs/heads/<branch>"`) — the sibling jq call on line 74 was hardened with `--arg` in Review 5 but the URL interpolations weren't; git ref names may legally contain `"`, `\`, `$`, `;`. Add a guard immediately before the `if git branch -D` line (inside the gate-passed `else`): `[[ "<branch>" =~ ^[A-Za-z0-9._/-]+$ ]] || { echo "STOP: branch name <branch> contains characters outside [A-Za-z0-9._/-] — refusing to build a gh api path from it"; }` and state that on that STOP the `-D` and both `gh api` calls for this branch must not run. Hardening only — every branch name here is copied from `gh pr list`'s `headRefName`.
- [ ] **Use `[[ ]]` in `worktree` Step 3's merge-tree status check** — `.claude/skills/worktree/SKILL.md` lines 42–43 — `if [ $status -eq 0 ]` / `elif [ $status -eq 1 ]` are the only POSIX single-bracket tests across both skills; every other test (`worktree` line 124, `compact-plans` lines 72, 83, 88) uses bash `[[ ]]`. Change both to `[[ $status -eq 0 ]]` / `[[ $status -eq 1 ]]`.
- [ ] **Make the classification-label bolding uniform in `compact-plans`** — `.claude/skills/compact-plans/SKILL.md` lines 32 ("For each Merged / Abandoned / Superseded plan") and 117 ("classified as Merged/Abandoned/Superseded") — **Live** is bolded on every mention (lines 28, 45, 99, 115) but its three sibling labels are bolded only at their defining bullets (lines 25–27). Bold all three on lines 32 and 117 (`**Merged** / **Abandoned** / **Superseded**`) to match Live's treatment.
- [ ] **Make the GREEN/YELLOW/RED bolding consistent within `worktree` Step 3's checkpoint sentence** — `.claude/skills/worktree/SKILL.md` line 51 — "any **RED** pair … Any **YELLOW** pair … Only GREEN and accepted-YELLOW features proceed" bolds two of the three verdict labels and then drops the bold on YELLOW within the same line. Bold `**GREEN**` and `accepted-**YELLOW**` in the final clause so all three read as the same set.
- [ ] **Add a one-line disambiguation from the built-in `EnterWorktree` tool to `worktree`** — `.claude/skills/worktree/SKILL.md`, the intro paragraph after `# Worktree` (line 9) — the built-in tool triggers on the word "worktree" in natural language and creates a single throwaway worktree under `.claude/worktrees/` with no F-ID/META-PLAN awareness; a prose request ("use a worktree to parallelize F3 and F7") could route there instead of here. Append one sentence: "This is distinct from the harness's built-in single-worktree isolation tool — invoke this skill explicitly as `/worktree <F-ID> …` for META-PLAN-driven parallel features."

- [ ] F15 (added 2026-07-15): Adopt kiosk-shell — remove the F1 auto-screen-blank and F2 touch-lock overlays from the app (their behavior is now provided by the pi-kiosk shell in front of the iframe-embedded app), and commit the embeddability guarantee (no `X-Frame-Options`/`frame-ancestors` denying the shell origin; recorded as an nginx.conf comment + README note). Runs only after pi-kiosk Phase 2 parity is verified on the Pi. See `plans/feature/kiosk-shell-extraction/kiosk-shell-extraction.md`.
- [ ] F11 (re-scoped 2026-07-15): Undo — allow the user to recover from accidental touch and chore completion. Bounded undo cache (1–2 levels sufficient if complexity/size limited); an undo is itself a write that must emit on the SSE bus. **Implemented in chores4irl**, surfaced through the pi-kiosk console via the `kiosk/v1` postMessage contract (chores4irl is the contract's first app-side client). Deferred until pi-kiosk Phase 4 delivers the contract — see `plans/feature/kiosk-shell-extraction/kiosk-shell-extraction.md`.
- [ ] F12 (re-scoped 2026-07-15): Redo — re-apply an action undone via F11; pairs with F11's cache (same 1–2-level bound). Same surfacing as F11 (`kiosk/v1` contract); depends on F11 + pi-kiosk Phase 4.
- [ ] F16 (added 2026-09-20): Status-bucketed midnight re-sort with red-quota escalation and Urgency weighting. Replace the single `duration × daysSince/frequency` score in `frontend/src/utils/choreSort.ts` so the unscrolled boot/midnight view shows a balanced spread of green / orange / red bars during normal operation, guarantees that the longer a chore is overdue the higher it ranks, and fills the fold with red only once the board has been neglected long enough. Spec: (1) bucket chores by the **same** red/orange/green status `computeBar` paints (sort and bar colour must never disagree); (2) rank inside each bucket — red by `overdueRatio = (daysSince − frequency) / frequency` descending (duration as tiebreak only), orange by `remainingRatio` ascending (closest to due first), green by `daysSince` ascending (most recently completed first, so the green slots show *recently accomplished* work); (3) fill a `FOLD`-sized above-the-fold window (≈ 8 bars, tunable constant) by quota — base red 4 / orange 2 / green 2, an under-filled bucket donates its slots to red first, then orange — and append the remaining reds, oranges, greens in that order; (4) deterministic escalation — `redQuota = min(FOLD, baseRed + pressure)` where `pressure` counts reds whose (urgency-scaled) `overdueRatio ≥ 1` (i.e. past 2× their frequency), so a long-neglected board opens the day all-red; (5) the chore's `urgency` field scales escalation **in the sort only** — a per-urgency multiplier on `overdueRatio` (low ×0.75 / medium-or-unset ×1 / high ×1.5) so a High chore climbs the red bucket faster and counts toward pressure sooner; bar colours / status thresholds in `choreBarMath` are untouched. Re-sort trigger unchanged: midnight (`useMidnightClock`) and day-simulation only, via the existing `simulatedDate` effect in `App.tsx`; screen unblank still does not re-sort; completing a chore still leaves it in place until midnight (the sticky-order rule from #8). `orderChores(chores, today)` keeps its signature so `App.tsx` is untouched. Touch set: `choreSort.ts`, `choreSort.test.ts`, a constants entry for `FOLD` / base quotas / urgency multipliers, README's "sort" paragraph.
- [ ] F17 (added 2026-09-20): Status-count strip — a thin, always-visible segmented bar directly under the room tabs (between `NavBar` and `DateNavigationBanner`) showing three counts for the **visible** list (room filter AND search filter applied, so selecting a room tab narrows it): **green = chores completed today** (`dateLastCompleted` falls on the displayed/simulated day), **orange = "soon"** (bar status orange), **red = overdue** (bar status red). Segment widths are proportional to the three counts; each non-zero segment carries its count centred in **bold white** text, and the strip is just tall enough for the smallest reasonably legible digit (≈ 16–20 px). Chores that are green-status but were completed on an earlier day are in no segment — so at the start of each morning the bar is orange + red only; if all three counts are 0 (e.g. every chore is green and none done today) render a **full green bar with a centred `0`**. Uses the **same colour tokens as the chore bars** (`bg-green-500` / `bg-orange-500` / `bg-red-500` via the shared status classifier). Counts and widths update **live** from `choreData`, not from the midnight-sorted order: completing an overdue chore lengthens green and shortens red proportionally; re-completing an already-green chore still increments "done today" and proportionally shrinks orange and red. Each segment exposes an accessible label (`"3 done today · 2 due soon · 5 overdue"`) for screen readers / `title`. Chosen over three alternatives (counts beside the date heading, counts under each room tab, badges in the search row) on 2026-09-20. **Standalone** — no collapse/hide toggle for the top section (explicitly kept separate; a future `/new-feature` if crowding persists). Touch set: new `components/nav/StatusCountStrip.tsx` (name TBD), a pure counting util beside `choreBarMath.ts`, one line in `App.tsx` wiring it to `searchFilteredChores` + `simulatedDate`, tests for both, README screenshot/description line.
- [ ] F18 (added 2026-09-20): Minimalist scroll-to-top button — a small, translucent, icon-only circle (lucide `ArrowUp`/`ChevronUp`, ≥ 44 px touch target, `aria-label="Scroll to top"`) **floating bottom-right over the chore list**, positioned against the scroll region's *frame* (not inside the scrolling `.overflow-y-auto` element, so it never scrolls away) and sitting clear above F5's sticky Add-Task deck and its 4 rem frosted overhang. **Hidden while the list is at or near the top**; fades in once the container has scrolled past a threshold (≈ one bar height, ~80 px) and fades out again at the top — so the boot/unblank view is unchanged. Tapping it smooth-scrolls the `.overflow-y-auto` container to 0 (instant under `prefers-reduced-motion`). Hidden state is non-interactive (`aria-hidden`, `tabIndex=-1`, `pointer-events-none`). Chosen 2026-09-20 over "inside the frosted deck" and "tap the date heading". Touch set: new `components/common/ScrollToTopButton.tsx` (name TBD), a `relative` wrapper (or equivalent) around the scroll region in `App.tsx` plus an `onScroll` handler/ref on the container, tests (visibility threshold, scroll call, reduced-motion, clearance from the deck), README line.
- [ ] F19 (added 2026-09-20): Lock-time view reset — when the touch lock engages (F2's 5-minute inactivity padlock, `isLocked` → `true`; **not** the F1 screen-blank, which the user explicitly excluded on 2026-09-20), the app resets its view behind the dimmed padlock so the next viewer always meets the canonical boot view: (1) the `.overflow-y-auto` list container scrolls to the top **instantly** (`scrollTop = 0` / `behavior: 'auto'` — never smooth; the app root is `inert` and nobody is watching), (2) the room tab resets to **All** (`selectedRoom → 'all'`), (3) the search filter clears (`searchQuery → ''` — the user chose to include it, 2026-09-20), and (4) the day simulator returns to **today** (`dayOffset → 0`), which — only when a simulation was active — re-sorts via the existing `simulatedDate` effect exactly as the Return-to-today button does (a day-simulation step, so consistent with the midnight/simulation-only re-sort rule). Fires on the lock *engage* transition **and — amended 2026-09-20 by F20 — on every subsequent 5-minute idle expiry while locked** (F20 keeps the inactivity timer running while locked because scroll/search/room/day-sim stay usable under the lock, so the view can drift; each expiry re-runs the reset and the wall stays canonical). A manual lock via the F20 indicator control is an engage and also resets. Never on unlock, never on blank, never on a repeated render while locked. **Trigger source: keyed directly off `useTouchLock`** — the `isLocked` engage transition plus the idle-expiry tick F20 exposes (chosen 2026-09-20 over an app-owned idle timer and over deferring to Phase 4; if F19 runs before F20 it keys off `isLocked` alone and F20 re-keys it). Governing principle recorded for the kiosk extraction: *the kiosk owns the inactivity timer; the app owns what "locked" means for it* — a `locked` signal (today `useTouchLock`, after `F15` the shell's `kiosk/v1` `kiosk-state { locked }` message) does not necessarily mean no interaction is possible, only that certain actions are guarded by the lock — so `F15` must keep an app-side `isLocked` state fed by the shell and re-key this reset to it (Phase 2 → Phase 4 gap to be resolved in `F15`'s plan). Touch set: one lock-transition effect in `App.tsx` (beside the existing force-close-dialogs effect), a `ref` on the `.overflow-y-auto` container (shared with `F18` — whichever runs first creates it), `App.touchLock` tests (scroll-to-0, room/search/day reset, no-op on unlock/blank, re-sort only when simulating), README line under the touch-lock paragraph.
- [ ] F20 (added 2026-09-20): Permissive touch lock — rework the shipped F2 lock so the padlock overlay appears **only when a disallowed action is attempted**, and the app stays usable under the lock. Principle (user, 2026-09-20): *the lock exists to prevent destructive behavior; creative behavior is accepted.* **Disallowed while locked** (without unlocking): completing a chore (single tap on a bar), edit and delete (the left/right swipes — bars do not move at all while locked, like today's day-simulation guard). **Allowed while locked**: scrolling (incl. F18's button), search (and its clear-✕), room tabs, day simulation (chevrons + Return-to-today), and **Add Task including its submit** (creating is not destructive). The app root is therefore **no longer `inert` for the lock** (blank keeps `inert`); the guard moves to the bar level (`isLocked` prop → tap/swipe route to a guard callback instead of complete/edit/delete). **Two ways to unlock:** (1) *blocked tap = first tap* — the blocked attempt surfaces `TouchLockOverlay` already in its awaiting-second-tap phase at the tap position; a second tap within the existing 1500 ms / 60 px window unlocks (the previously-blocked action is then available; no follow-up tap → overlay fades after the window, nothing else changes); (2) the persistent top-left `TouchLockIndicator` becomes a **control** (≥ 44 px target): a **single tap** unlocks while locked and **locks immediately** while unlocked (manual lock instead of waiting 5 min). The indicator is the *kiosk-level state indicator/control* — it is what F15 raises to the pi-kiosk shell — while the in-list padlock flash is the *app-level response* to that state. The inactivity timer keeps running while locked (each 5-min expiry is a tick F19 consumes) and re-arms on any activity as today. Consequences recorded: F19's trigger amended (re-fires on each idle expiry while locked; manual lock = engage); F15 / pi-kiosk DD-2 can no longer be a shell overlay that blocks all iframe input — the shell owns the timer + lock *state* and forwards it (`kiosk-state { locked }`), the app owns the guard + padlock feedback and sends `lock-request { locked }` back (unlock via the double-tap path; lock/unlock via the indicator, which the shell renders) — a `kiosk/v1` contract addition to be specified when F15 plans. Touch set: `hooks/useTouchLock.ts` (timer runs while locked, exposes `lock()` + an idle-expiry tick), `components/common/TouchLockOverlay.tsx` (mount-on-attempt with a seeded first tap; `justRelocked` entrance repurposed/removed), `TouchLockIndicator.tsx` (→ button), `components/chore/ChoreTimerBar.tsx` + `ChoreList` (`isLocked` + `onGuardedAttempt` props; no dimming), `App.tsx` (drop `inert` for lock, keep for blank; keep the force-close-dialogs effect on engage), the F2 test suites reworked + new guard tests, README's touch-lock paragraph rewritten.
- [ ] F21 (added 2026-09-20; promotes the `F2-L` follow-up list confirmed 2026-07-08): Add/edit form polish + date-math fix — one feature covering the four still-open items of that list (its #2, "the Add modal doesn't close after a successful add", was verified already on `main` — `handleAddChore` has called `setShowForm(false)` on success since #15 — and is dropped). **(1) Date-math fix — the bug reported 2026-09-20:** a chore created with *Last Completed* = today is stored/rendered as the day **before** ("1 day ago", one day lower in every `daysSince`). Root cause: `ChoreForm.tsx`'s submit builds `new Date(formData.dateLastCompleted)` from the bare `yyyy-mm-dd` input string, which `Date` parses as **UTC midnight**, while `choreSort.ts` / `ChoreTimerBar.tsx` diff `startOfDay(chore.dateLastCompleted)` against **local** midnight — in any timezone behind UTC that instant is the previous local day. Mirror bug on the way *in*: `choreToFormState` fills the edit form with `date.toISOString().slice(0, 10)` (the UTC date), so an evening completion in a behind-UTC zone opens for edit showing *tomorrow*. Fix **at the form boundary only, both directions**: parse with `date-fns` `parse(str, 'yyyy-MM-dd', new Date())` (local midnight) and format with `format(date, 'yyyy-MM-dd')` (local); the `daysSince` expressions, `computeBar`, `completeChore`'s `new Date()` and the backend (which stores the ISO instant) are untouched. Tests pin the behaviour with `vi.stubEnv('TZ', …)`/`process.env.TZ` in a behind-UTC and an ahead-of-UTC zone. **(2) Date default:** in add mode *Last Completed* defaults to today's local date (`format(new Date(), 'yyyy-MM-dd')`); edit mode keeps the chore's date. **(3) Room default:** in add mode *Room* pre-fills with the active room tab when `selectedRoom !== 'all'`, blank when *All* (a `defaultRoom` prop from `App.tsx`; the `<datalist>` and clear-✕ unchanged). **(4) Success feedback + unified toast (chosen 2026-09-20 over `sonner` and over a green variant of the top banner):** a new `components/common/Toast.tsx` (`{ tone: 'success' | 'error'; message: string; onDismiss }`), a bottom-centred pill **fixed against the viewport/frame, never inside `.overflow-y-auto`**, sitting above F5's deck + 4 rem overhang (like F18's button, which keeps bottom-right), `role="status"`, `aria-live="polite"`, no new dependency. Success tone (green) auto-dismisses after ≈ 2.5 s — "Added «name»" / "Saved «name»" / "Deleted «name»" from the existing add/edit/delete handlers on *success only* (optimistic edit/delete toast on the resolved request, not the optimistic update). Error tone (red) **replaces the red top-of-page error strip** in `App.tsx` (same messages; stays until dismissed via its ✕ / tap, so the list no longer shifts down under an error). One toast at a time — a new one replaces the current. Toast state is not user input, so it is **not** added to `isRepullGated()`. Renders under the lock (`F20` allows Add Task while locked) and below the overlays (`z-[80]` < lock `z-[90]` < blank `z-[100]`). Touch set: `components/form/ChoreForm.tsx` (+ `ChoreForm.test.tsx`), `App.tsx` (`defaultRoom`, toast state, error strip removed; `App.test.tsx` error assertions re-pointed), new `Toast.tsx` + test, README (form defaults + feedback line).

## Settings / device-control panel — MIGRATED to pi-kiosk (2026-07-15)

The device-control panel ("console") and its controls no longer ship inside chores4irl.
Per the kiosk-layer extraction decision (design doc:
`plans/feature/kiosk-shell-extraction/kiosk-shell-extraction.md`), the screen belongs to a
standalone **`rehankalu/pi-kiosk`** repo — a kiosk-shell web page (Chromium's kiosk
target, embedding the displayed app in an iframe) plus a host-side kiosk-agent service —
so the console works regardless of which application is displayed. Disposition of the
former section's items:

- **Panel container (F3)** → pi-kiosk shell console (same design: gear↔X toggle,
  single-row overlaid icon banner; placeholder-first rollout at the new repo's discretion).
- **Brightness (F7)**, **manual screen blank/wake toggle (F8)**, **Restart (F10)** →
  pi-kiosk agent controls, called directly over localhost HTTP (the old
  frontend→backend→state-file→host-agent bridge design is superseded).
- **Auto screen blank/wake settings (F9)** → pi-kiosk settings UI editing agent-owned
  config (`PATCH /api/config`), which also carries F1's formerly hardcoded 9pm/6am times.
- **Rotate (F13)** → pi-kiosk agent; the host-side decisions of
  `plans/completed/rotate-screen-button/rotate-screen-button.md` are harvested (see that
  file's supersession banner and the design doc's DD-8).
- **Undo / Redo** → remain chores4irl features: re-listed above as F11/F12 (re-scoped onto
  the `kiosk/v1` contract).

- [ ] F15 (added 2026-07-15): Adopt kiosk-shell — remove the F1 auto-screen-blank and F2 touch-lock overlays from the app (their behavior is now provided by the pi-kiosk shell in front of the iframe-embedded app), and commit the embeddability guarantee (no `X-Frame-Options`/`frame-ancestors` denying the shell origin; recorded as an nginx.conf comment + README note). Runs only after pi-kiosk Phase 2 parity is verified on the Pi. See `plans/feature/kiosk-shell-extraction/kiosk-shell-extraction.md`.
- [ ] F11 (re-scoped 2026-07-15): Undo — allow the user to recover from accidental touch and chore completion. Bounded undo cache (1–2 levels sufficient if complexity/size limited); an undo is itself a write that must emit on the SSE bus. **Implemented in chores4irl**, surfaced through the pi-kiosk console via the `kiosk/v1` postMessage contract (chores4irl is the contract's first app-side client). Deferred until pi-kiosk Phase 4 delivers the contract — see `plans/feature/kiosk-shell-extraction/kiosk-shell-extraction.md`.
- [ ] F12 (re-scoped 2026-07-15): Redo — re-apply an action undone via F11; pairs with F11's cache (same 1–2-level bound). Same surfacing as F11 (`kiosk/v1` contract); depends on F11 + pi-kiosk Phase 4.
- [ ] F16 (added 2026-09-20): Status-bucketed midnight re-sort with red-quota escalation and Urgency weighting. Replace the single `duration × daysSince/frequency` score so the unscrolled boot/midnight view shows a balanced red / orange / green spread during normal operation, an overdue chore always ranks higher the longer it is overdue, and the fold fills with red only once the board has been neglected long enough (deterministic escalation). Bucket by the same status `computeBar` paints; the chore's `urgency` scales escalation in the sort only. Re-sort trigger unchanged (midnight and day-simulation only). Formulas, quotas, multipliers and touch set: `plans/META-PLAN.md` § F16.
- [ ] F17 (added 2026-09-20): Status-count strip — a thin, always-visible segmented bar under the room tabs showing three counts for the visible list: green = completed today, orange = due soon, red = overdue, segment widths proportional, bold white count in each non-zero segment, all-zero → full green bar with a `0`. Same colour tokens as the chore bars; updates live from `choreData` (not the midnight order). Chosen 2026-09-20 over counts beside the date heading, under each room tab, or badges in the search row; standalone (no collapse toggle for the top section). Details and touch set: `plans/META-PLAN.md` § F17.
- [ ] F18 (added 2026-09-20): Minimalist scroll-to-top button — a small translucent icon-only circle floating bottom-right over the chore list, fixed against the scroll region's frame (never scrolls away), clear of F5's sticky Add-Task deck; hidden at/near the top, fades in after ≈ one bar height of scroll, smooth-scrolls the container to 0 (instant under `prefers-reduced-motion`). Chosen 2026-09-20 over placing it inside the frosted deck or tapping the date heading. Details and touch set: `plans/META-PLAN.md` § F18.
- [ ] F19 (added 2026-09-20): Lock-time view reset — when F2's 5-minute touch lock engages (not the F1 screen-blank, which the user excluded), instantly scroll the list to the top, reset the room tab to All, clear the search filter (user chose to include it) and return the day simulator to today, so the next viewer meets the canonical boot view. Keyed directly off `useTouchLock` (chosen over an app-owned idle timer and over deferring to Phase 4). Amended by F20 the same day: also re-fires on every 5-minute idle expiry while locked and on a manual lock, and each tick first closes an Add modal left open under the lock. Governing principle for the kiosk extraction: the kiosk owns the inactivity timer; the app owns what "locked" means for it. Details and touch set: `plans/META-PLAN.md` § F19.
- [ ] F20 (added 2026-09-20): Permissive touch lock — rework the shipped F2 lock so the padlock overlay appears only when a disallowed action is attempted and the app stays usable under the lock. Principle (user, 2026-09-20): the lock exists to prevent destructive behavior; creative behavior is accepted. Blocked while locked: complete, edit, delete. Allowed: scrolling, search, room tabs, day simulation, Add Task including submit. Unlock either by a second tap after a blocked tap (existing 1500 ms / 60 px window) or by a single tap on the top-left indicator, which becomes a lock/unlock control. The inactivity timer keeps running while locked (each expiry is a tick F19 consumes). Consequences: F19's trigger amended; F15 / pi-kiosk DD-2 becomes shell-owns-timer-and-state, app-owns-guard-and-feedback (a `kiosk/v1` addition specified when F15 plans). Details and touch set: `plans/META-PLAN.md` § F20.
- [ ] F21 (added 2026-09-20; promotes the `F2-L` follow-up list confirmed 2026-07-08): Add/edit form polish + date-math fix. (1) Bug reported 2026-09-20: a chore created with *Last Completed* = today renders as the day before — `ChoreForm` parses the bare `yyyy-mm-dd` input as UTC midnight while the day math uses local midnight (mirror bug on the way into the edit form); fix at the form boundary only, both directions, with TZ-pinned tests. (2) *Last Completed* defaults to today in add mode. (3) *Room* pre-fills with the active room tab. (4) Success feedback via a new unified bottom-centred `Toast` (success auto-dismisses; error tone replaces the red top-of-page strip), chosen 2026-09-20 over `sonner` and over a green variant of the banner. The list's "modal doesn't close after add" item was verified already fixed on `main` and dropped. Details and touch set: `plans/META-PLAN.md` § F21.

## Settings / device-control panel — MIGRATED to pi-kiosk (2026-07-15)

The device-control panel ("console") and its controls no longer ship inside chores4irl.
Per the kiosk-layer extraction decision (design doc:
`plans/feature/kiosk-shell-extraction/kiosk-shell-extraction.md`), the screen belongs to a
standalone **`rehankalu/pi-kiosk`** repo — a kiosk-shell web page (Chromium's kiosk
target, embedding the displayed app in an iframe) plus a host-side kiosk-agent service —
so the console works regardless of which application is displayed. Disposition of the
former section's items:

- **Panel container (F3)** → pi-kiosk shell console (same design: gear↔X toggle,
  single-row overlaid icon banner; placeholder-first rollout at the new repo's discretion).
- **Brightness (F7)**, **manual screen blank/wake toggle (F8)**, **Restart (F10)** →
  pi-kiosk agent controls, called directly over localhost HTTP (the old
  frontend→backend→state-file→host-agent bridge design is superseded).
- **Auto screen blank/wake settings (F9)** → pi-kiosk settings UI editing agent-owned
  config (`PATCH /api/config`), which also carries F1's formerly hardcoded 9pm/6am times.
- **Rotate (F13)** → pi-kiosk agent; the host-side decisions of
  `plans/completed/rotate-screen-button/rotate-screen-button.md` are harvested (see that
  file's supersession banner and the design doc's DD-8).
- **Undo / Redo** → remain chores4irl features: re-listed above as F11/F12 (re-scoped onto
  the `kiosk/v1` contract).

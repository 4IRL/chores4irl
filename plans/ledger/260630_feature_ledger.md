- [ ] Remove 'Details' and 'Long-term task' toggle from the Add Task form. Propagate the removal from the database and other associated schema related to these fields.
- [ ] Include a type-as-you-search dropdown box for 'Room' on Add Task and Edit Task forms. native <datalist> is acceptable
- [ ] Make 'Add Task' button deck transparent blur background, allowing the chores list visible slightly visible beneath for a more modern, fluid feel. The button will remain locked at the bottom and opaque as the chores list scrolls, but you should be able to see the chore bars on the sides of the button.
- [ ] Explore options by which local users can access a URL alias rather than an IP address and port. e.g. users connected to the local network can type 'C4I' in their browser, instead of [local_IP_address]:[port]
- [ ] Add a dynamic chore type-as-you-search text input filter, located persistently at the top of the chores list, regardless of which room is highlighted. Chores are filtered out of visibility based on the substring entered by the user. This is meant to allow users to quickly find the desired chore to update once the list becomes unwieldly. Include a magnifying glass icon and placeholder text "Search for a chore" so the input field's intent is clear.
- [ ] Swap swipe direction behaviors: swipe left to edit, swipe right to delete. When swiping, the task bar should shift to reveal an action indicator: a yellow background, white outline pencil icon when swiping left to indicate edition, and a red background, white outline trash bin icon to indicate deletion when swiping right. Require the user to swipe past 25% width from the start of the swipe to confirm activation of the behavior. Else, the user should be able to let go and have the task bar animate back to nominal state

## Settings / device-control panel (added 2026-06-28)

The rotate-screen control will live inside a new collapsible **settings panel** rather than as a bare NavBar button (see `plans/feature/rotate-screen-button/`). The panel is a single NavBar toggle (a gear/settings icon that swaps to an 'X' when open) which expands into a **single-row overlaid banner of kiosk/device control icons**. The rotate control is wired and functional; the controls below ship initially as **disconnected, optimistic icon placeholders** (rendered, labelled, inert — no backend) for future work to connect.

- [ ] **Brightness control** — adjust the kiosk display backlight from the settings panel. Likely a host-bridge mirroring rotate (frontend → backend writes a brightness state file → host agent applies it via the backlight sysfs interface or `ddcutil` for the HDMI panel). Placeholder icon (`Sun`) ships first, disconnected.
- [ ] **Screen blank / wake toggle** — manually blank (sleep) or wake the display on demand from the panel, complementing the auto screen-blank-disable already configured on the Pi. Placeholder icon (`MonitorOff`) ships first, disconnected.
- [ ] **Restart** — trigger a controlled restart from the panel — of the app stack (`docker compose restart`) and/or the Pi host — behind a confirmation dialog. Likely another host-bridge action. Placeholder icon (`Power`) ships first, disconnected.
- [ ] **Undo** — allow the user to recover from accidental touch and chore completion. We should consider how big of an undo cache to practically allow. If complexity or cache size limited, 1-2 undos should be considered sufficient.
- [ ] **Redo** — re-apply an action undone via the undo feature; pairs with the undo cache above. Same bounded cache consideration (1–2 steps if complexity/size limited). Placeholder icon (`Redo2`) ships first, disconnected; undo (`Undo2`) likewise relocates into this panel.
- [ ] **Rotate** — rotate screen button on the UI to dynamically allow the user to change the touch behavior and display orientation. This way, regardless of how the screen is mounted, the user can adjust, rather than requiring a developer to adjust the screen

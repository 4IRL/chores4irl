## Summary

- **F4 removes the *Details* text field and the *Long-term task* checkbox** end to end: the shared `Chore` type (`types/SharedTypes.d.ts`), the chore form (`ChoreForm.tsx`), the backend data-access layer (`createChore` + `updateChore` + `rowToChore` in `chores.ts`), the SQLite schema and seed (`db.ts`), the frontend sort util (`choreSort.ts`), the dead reference file `frontend/src/assets/database.ts`, and the root `README.md`.
- **First boot migration in `db.ts`.** `dropLegacyChoreColumns(db)` runs at module load between `CREATE TABLE IF NOT EXISTS` and the seed guard: it reads `pragma table_info('chores')` inside a `BEGIN IMMEDIATE` transaction (`db.transaction(...).immediate()`) and issues one `ALTER TABLE chores DROP COLUMN` per still-present legacy column, so the guard and the drops share the same write lock and a second boot is a no-op. It is deliberately **crash-loud** — no try/catch — so a failed `ALTER` aborts the backend before `listen` instead of masking the failure.
- **`urgency` is retained** in the schema, `Chore` type, form, and data access.
- **F3-L (Room datalist) and F14 (clear-✕ on Name/Room/search) wiring is untouched** — `ClearButton.tsx`, `ChoreSearchInput.tsx`, and `FormField.tsx` have no diff against `main`, and `ChoreForm.tsx` shows only deletions.
- **Test deltas.** Backend 32 → 43 tests: 7 new `db-migration.test.ts` cases (idempotency on `:memory:`, boot wiring against a legacy temp file) plus 4 stale-key tests (POST/PUT in `routes.test.ts`, `createChore`/`updateChore` in `chores.test.ts`) proving legacy `details`/`longTermTask` keys are dropped, not rejected. Frontend 252 → 252: the `choreSort` short/long-term partition test is replaced by a stale-flag-ignored test, and an F4 absence test (`Details` and `Long-term task` both absent from the form) is added in `ChoreForm.test.tsx`.

## Behaviour change

Chores formerly flagged long-term now interleave by duration-weighted score instead of being pinned to the bottom of the list. This is **effective from the Step 1 commit** (`7586944`, backend data access), not Step 3's `choreSort` collapse: once `rowToChore` stops emitting `longTermTask`, the untouched `orderChores` already saw every chore as short-term (`!c.longTermTask` is true for all), so a bisect of the user-visible sort change lands on Step 1. Step 3 then removed the now-dead partition code.

## Pi deploy runbook

All commands run on the Pi, unsandboxed — the LAN is unreachable from the sandbox; `cd ~/chores4irl` first so `docker compose` finds the compose file:

1. **Inspect what the migration will discard** (irreversible): `docker compose exec backend node -e "const D=require('better-sqlite3'); console.log(new D(process.env.DB_PATH,{readonly:true}).prepare('SELECT id, name, details FROM chores WHERE details IS NOT NULL').all())"` — fold anything worth keeping into the chore name via the Edit form first. (Runs against the still-running pre-F4 container; the container's `/app/node_modules` has `better-sqlite3` hoisted by `npm ci --workspace backend`, and `node -e` evaluates as CommonJS despite the root `"type": "module"`, so `require` resolves — same as the repo-root one-liner in Step 2.)
2. **Snapshot**: `sudo systemctl start chores4irl-backup.service && ls -la ~/backups` (README "Backup strategy" validation command). The unit supplies the mandatory `BACKUP_RSYNC_DEST`; the bare `bin/chores4irl-backup.sh` exits 1 without it. The script writes `~/backups/chores4irl-<YYYYmmdd-HHMMSS>.db` (`BACKUP_DIR` default `/home/rmilarachi/backups`) **before** the off-Pi `rsync`, so the local file exists even if the rsync leg fails — check `journalctl -u chores4irl-backup.service` and note the exact filename for step 6.
3. **Deploy**: ship the tarball per README "Updating an existing Pi deployment" (`git archive … HEAD` on the laptop, `scp`, `tar -xzf … -C ~/chores4irl`), then `docker compose up -d --build`. The migration runs on the backend container's first boot, between `CREATE TABLE IF NOT EXISTS` and the seed guard.
4. **Verify**: `docker compose exec backend node -e "const D=require('better-sqlite3'); console.log(new D(process.env.DB_PATH,{readonly:true}).pragma(\"table_info('chores')\").map(c=>c.name))"` → the 7 columns (`id, name, room, date_last_completed, duration, frequency, urgency`); `curl -s http://localhost/api/chores` (the backend publishes no host port — only nginx on `:80` is reachable; from inside the container `docker compose exec backend node -e "fetch('http://127.0.0.1:3000/api/chores').then(r=>r.json()).then(j=>console.log(JSON.stringify(j)))"`) returns the household's rows with no `details`/`longTermTask` keys. If the frontend never comes up (`depends_on: condition: service_healthy`), run `docker compose logs backend` — a thrown `ALTER` aborts boot before `listen` **by design** (crash-loud migration policy, no try/catch — see *Decisions resolved during planning*), the `HEALTHCHECK` never passes, and `restart: "on-failure:5"` gives up after five tries.
5. **Reload the kiosk page** (or reboot the Pi) so Chromium picks up the new bundle — until then the old form still shows Details/Long-term inputs whose values the new backend silently drops (`createChore`/`updateChore` build explicit named-param literals, so the stale keys are ignored, not rejected).
6. **Rollback** — the pre-F4 image is **NOT** compatible with the migrated DB: its `createChore`/`updateChore` SQL still names `details`/`long_term_task`, so against a 7-column table every POST/PUT would 500 (`table chores has no column named details` / `no such column: details`) while GET keeps working. (The reverse direction is safe — the F4 image's trimmed SQL works against a 9-column DB, which is what Step 1 relies on.) A rollback must therefore restore the pre-deploy snapshot **together with** the old image:
   - `docker compose stop backend frontend`
   - find the volume: `docker volume ls | grep chores-data` — `docker-compose.yml` sets no `name:`, so compose prefixes the project name derived from the directory (`~/chores4irl` → `chores4irl_chores-data`) _(derived, not verified: `deploy/pi/chores4irl-backup.service` `ExecStart=` and the script's `COMPOSE_FILE` default both pin `/home/rmilarachi/chores4irl/`, so the compose project name — and thus `chores4irl_chores-data` — is what the working weekly backup already relies on; the Pi's own `chores4irl.service` unit is not in the repo and could override it with `COMPOSE_PROJECT_NAME`/`-p`, so confirm with `docker volume ls --filter name=chores-data --format '{{.Name}}'` and use whatever it prints in the next command)_
   - `docker run --rm -v chores4irl_chores-data:/data -v /home/rmilarachi/backups:/b alpine sh -c "cp /b/chores4irl-<YYYYmmdd-HHMMSS>.db /data/data.db && rm -f /data/data.db-wal /data/data.db-shm && chown 1000:1000 /data/data.db"` (the runtime image runs as `USER node`, uid/gid 1000 in `node:20.18.0-bookworm-slim`; the stale `-wal`/`-shm` must go or SQLite would replay the post-migration WAL over the restored file)
   - on the laptop re-archive the pre-F4 commit — `git archive --format=tar.gz -o /tmp/chores4irl-src.tar.gz <pre-F4 sha>` — `scp` it over, re-extract into `~/chores4irl`, then `docker compose up -d --build` (leftover F4-only files such as `backend/src/__tests__/db-migration.test.ts` are harmless: `.dockerignore` excludes `**/__tests__` from the build context)
   - prefer the step-2 snapshot over the weekly `chores4irl-backup.timer` ones (Sundays 03:00; they may predate the week's edits). Conversely the NEW image boots cleanly against an un-migrated snapshot (the `pragma table_info` guard re-runs the `DROP`s), so re-running the migration after a restore is safe.

## META-PLAN reconciliation

**Reconciled deviation from META-PLAN F4 "Expected end state" bullet 1.** The literal "`grep -rn "longTermTask\|long_term_task" backend frontend types` returns **no matches**" is unsatisfiable by construction — the migration must emit `long_term_task` inside its `ALTER TABLE … DROP COLUMN` SQL, and META-PLAN's own "Test-suite deltas" require an idempotency test that recreates the legacy DDL. Amended wording to record when `/run-feature` folds F4 into META-PLAN (Phase C):

> `grep -rn "longTermTask\|long_term_task" backend frontend types` matches only (a) the `LEGACY_CHORE_COLUMNS` migration list + comment in `backend/src/db.ts`, (b) `backend/src/__tests__/db-migration.test.ts` (legacy DDL + legacy INSERTs), (c) the stale-client tests in `backend/src/__tests__/chores.test.ts`, `backend/src/__tests__/routes.test.ts`, `frontend/src/__tests__/components/ChoreForm.test.tsx`, and `frontend/src/__tests__/utils/choreSort.test.ts`; nothing in `app.ts`, `chores.ts`, `SharedTypes.d.ts`, or any non-test frontend file.

`plans/META-PLAN.md` is not edited on this branch beyond the F4 Status-ledger row. `/run-feature` Phase C deletes the F4 section, so the amended wording lands where the fact survives: the Baseline **Domain model** line (`plans/META-PLAN.md:255`) and the Chain-integrity cumulative-invariant bullet (`plans/META-PLAN.md:843`), both reworded to the same "matches only (a)…(c)" allow-list.

`fixtures/chore.ts` (listed in META-PLAN's F4 'Test-suite deltas') needed no change — `makeChore` never defaulted `details`/`longTermTask`, so `/run-feature` Phase C can lift it straight into the Baseline Domain-model line (`plans/META-PLAN.md:255`) and `plans/META-PLAN.md:843`.

## Verification

- `npm test --workspace backend` — 5 files / 43 tests, all green.
- `npm test --workspace frontend` — 29 files / 252 tests, all green.
- `env -u PLAYWRIGHT_BASE_URL CI=1 npx playwright test e2e/smoke.spec.ts` — 14 passed.
- `npx tsc --noEmit -p backend/tsconfig.json`, `npx tsc --noEmit -p frontend/tsconfig.json`, `npm run lint` — all clean (local-only gates — CI runs only the Vitest suites and e2e).
- Worktree `data.db` migrated in place by the first post-`db.ts` test run: `pragma table_info('chores')` went from 9 → 7 columns with all 10 seed rows preserved; the smoke run then exercised the idempotent no-op path against the already-migrated file.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_0143xtEatGKycuPnJEZWmREZ

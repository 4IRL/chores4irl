# chores4irl — installed by deploy/pi/set-hostname.sh
# Keep the hostname set by hostnamectl and stop cloud-init re-rendering
# /etc/hosts from its (possibly cached) user-data on every boot.
preserve_hostname: true
manage_etc_hosts: false
